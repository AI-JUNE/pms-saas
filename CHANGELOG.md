# PRISM PMS — 변경 이력 (CHANGELOG)

> 야간 자동 개발이 매 실행마다 최신 항목을 **맨 위에** 추가합니다.
> 아침에 `배포.ps1` 실행 → GitHub 푸시 → Vercel 자동배포.

## 2026-09-03 (배치 151 — 배포 대기, 상용 필수: 입력검증 + 공개 API rate limit)
- ★ **COMMERCIAL_READINESS 상위 2건 처리** — `표준 에러 응답 전 API 통일 + 입력검증`, `rate limit 공개 API 적용`. 사전 점검 결과 API 71건 중 직접 `NextResponse.json`을 쓰는 건 health 하나뿐(의도적 스키마)이고 나머지는 전부 `handle()`/`ApiError` 경유였음 → 실제 공백은 **입력검증 부재**(지금까지 `required` 유무만 검사, 타입·길이·범위 무제한)였고 이를 메웠다.
- ⑨ **신규 `src/lib/validate.ts`(순수 모듈)** — 필드명 규약으로 종류를 추론(`*Date`/`*Start`/`*End`→날짜, progress→0~100 퍼센트, qty·sortOrder·parentId 등→정수, budget·unitPrice·공수·storyPoints 등→숫자, description·content·steps 등→긴텍스트 20000자, email→형식+254자, 그 외→텍스트 500자). null·undefined·빈문자열은 "비움"으로 통과시켜 기존 정상 입력 호환을 유지하고, 명백한 오류(숫자 아님·날짜 아님·객체 주입·과도한 길이·범위 초과)만 차단. `configs.ts` 수정 없이 전 리소스에 일괄 적용된다.
- ⑨ **crud.ts 연동** — collection POST·item PATCH에서 화이트리스트 통과 값에 `validateValues()` 적용, 위반 시 표준 `{ok:false, code:'VALIDATION', message, fields:[{field,code,message}]}` 반환. 기존 `필수 항목을 입력하세요`도 어느 필드가 빈지 `fields[]`로 함께 내려주도록 개선. — src/lib/validate.ts, src/lib/crud.ts
- ⑨ **공개 API rate limit** — `lib/ratelimit.ts`에 프리셋 4종(publicHealth 120/분, publicPlans 60/분, publicAsset 60/분, billingWebhook 120/분)과 `rateLimitResponse()`(handle 래퍼가 없는 라우트용 표준 429 + `Retry-After`) 추가. `api/health`·`api/brand-logo`(외부 fetch 동반이라 남용 비용이 큼)·`api/billing/plans`·`api/billing/webhook`에 적용 → 기존 auth/login·register·client-errors·billing/checkout와 합쳐 **인증 불필요 엔드포인트 전건 커버**. 업타임 모니터·PG 재시도를 막지 않도록 한도는 넉넉히 잡았다. 네 라우트 모두 HTTP 메서드·`dynamic` 외 export 0건(Vercel 빌드 규칙 준수). — src/lib/ratelimit.ts, src/app/api/{health,brand-logo,billing/plans,billing/webhook}/route.ts
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주 — `error TS` 0건**. `node --test tests/*.test.ts` **113/113 통과**(신규 14건: 규약 추론, 빈값 통과, 날짜 ISO/YYYY-MM-DD 허용·쓰레기 차단, 숫자문자열 허용·정수 소수 차단, 퍼센트 0~100 경계, 텍스트/긴텍스트 길이 경계, 이메일 형식, 객체 주입 차단, 묶음 검증·요약 메시지, rateLimitResponse 429 본문·Retry-After, 프리셋 정의). 작업 전 src 백업(/tmp/bak_1788399563). 임의 KPI 수치 미기재.
- ⚠ rate limit은 **인메모리 단일 인스턴스** 기준(서버리스 다중 인스턴스에서는 인스턴스별 카운트). 공유 스토어(Upstash Redis 등) 승격은 **[활성화 승인 필요]**.
- ⏭ 다음: `접근·감사 로그(관리 기능 접근 이력)`, 이어서 `백업·복구 절차 RUNBOOK.md`.

## 2026-09-03 (배치 150 — 배포 대기, 상용 필수: /health 확장)
- ★ **COMMERCIAL_READINESS 최상위 1건 처리** — `/health 확장`. 기존 라우트는 db 핑·version·uptime만 반환했고 (a)커밋 해시·배포 환경 메타 부재, (b)외부 의존성(결제 프로바이더) 상태 미노출, (c)**DB 에러 메시지를 가공 없이 공개**해 연결 문자열 유출 위험이 있었음.
- ⑨ **판정 로직을 순수 모듈로 분리** — 신규 `src/lib/health.ts`에 `sanitizeError()`(URL·user:pass@host·32자 이상 토큰 마스킹 + 160자 절단), `shortCommit()`(7자리), `buildInfo()`(APP_VERSION / VERCEL_GIT_COMMIT_SHA·GIT_COMMIT_SHA·COMMIT_SHA·SOURCE_VERSION / 브랜치 / VERCEL_ENV·NODE_ENV / VERCEL_REGION, 없으면 null — 추측값 미기재), `summarize()`(required 실패=down·503, optional 실패만이면 degraded·200), `buildHealthBody()` 배치. 라우트는 조립만 담당.
- ⑨ **의존성 체크 3종** — `db`(select 1, **required**), `billing`(외부 API 실호출 없이 `billingStatus()` 설정 완비 여부만 확인. 테스트 모드에서는 미설정이 정상이라 live일 때만 판정, optional), `monitoring`(MONITORING_ENABLED·SENTRY_DSN·ALERT_WEBHOOK_URL의 **설정 여부 boolean만** 노출 — DSN·URL 값 자체는 미노출, optional). 공개 엔드포인트 원칙상 자격증명·내부 호스트는 어떤 경로로도 나가지 않는다. route.ts는 GET·dynamic 외 export 0건(Vercel 빌드 규칙 준수). — src/lib/health.ts, src/app/api/health/route.ts
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주 — `error TS` 0건**. `node --test tests/*.test.ts` **99/99 통과**(신규 10건: 연결 문자열·토큰 마스킹, 절단 길이, 빈 입력 폴백, shortCommit, buildInfo 메타 추출/미추측, summarize down·degraded 분기, statusCode 매핑, 응답 스키마, required/optional별 ok 플래그). 작업 전 src 백업(/tmp/bak_1788396348). 임의 KPI 수치 미기재.
- ⏭ 다음: `표준 에러 응답 전 API 통일 + 입력검증`(라우트 다수 수정 예상 — 별도 배치 권장), 이어서 `rate limit 공개 API 적용`.

## 2026-09-02 (배치 149 — 배포 대기, 상용 필수: 전역 에러 캡처 + 구조화 요청 로깅)
- ★ **COMMERCIAL_READINESS 최상위 2건 처리** — ①에러 모니터링 ②구조화 로깅. logger.ts에 이미 JSON 로깅·captureError 기반은 있었으나 (a)라우트 밖 예외를 잡는 전역 훅, (b)클라이언트 렌더 오류 수집, (c)요청 단위 상관관계·소요시간 기록이 전부 부재했음.
- ⑨ **전역 에러 캡처 + 알림 훅** — logger.ts에 `installGlobalErrorHandlers()`(process unhandledRejection·uncaughtException → captureError, 중복등록 가드)와 `notifyAlert()`(ALERT_WEBHOOK_URL 웹훅, PII 제거 후 발신, await 안 함) 추가. instrumentation.ts register() 최상단에서 설치해 부팅 단계 오류까지 포착. 클라이언트는 `app/global-error.tsx`(루트 에러 경계, html/body 직접 렌더, 테라코타 #be5535 안내 화면 + 다시시도/홈) → `POST /api/client-errors`로 보고(허용 필드 화이트리스트·길이 제한·IP 분당 30회 rate limit·항상 202). **활성화 스위치 기본 OFF** — MONITORING_ENABLED=true 且 SENTRY_DSN/ALERT_WEBHOOK_URL 설정 시에만 외부 발신, 미설정이면 완전 no-op **[실발신 활성화는 승인 필요]**. 신규 route.ts는 POST·dynamic 외 export 없음(Vercel 빌드 규칙 준수). — src/lib/logger.ts, src/instrumentation.ts, src/app/global-error.tsx, src/app/api/client-errors/route.ts
- ⑨ **구조화 로깅(요청ID·소요시간·에러코드) + PII 미기록** — http.ts `handle(fn, req?)`로 확장(2번째 인자 선택 · 기존 28개 호출부 무수정 하위호환). req 전달 시 requestId(수신 x-request-id 재사용 — 길이 64·[A-Za-z0-9._-] 검증 통과분만)·method·path·status·durMs·code를 한 줄 JSON으로 기록하고 응답에 `x-request-id` 헤더 부착, status 5xx=error/4xx=warn/그외 info. `requestContext()`는 pathname만 취해 쿼리스트링(PII 유입 경로)을 로그에서 배제. logger.ts `redact()`가 password·token·cookie·email·phone·billingKey 등 26개 필드명(대소문자·구분자 무시)을 '[redacted]'로 치환하며 emit 경로 전체에 적용. — src/lib/http.ts, src/lib/logger.ts
- ⑨ **[승인 완료 2026-09-02] Sentry 실전송 결선** — 사용자 승인(범위: 배치149 모니터링 한정, 스위치는 운영자 직접 설정)에 따라 forwardToSink 스텁을 실코드로 교체. `parseDsn()`(DSN → envelope 엔드포인트 파싱)·`buildEnvelope()`(NDJSON 3줄 이벤트 생성) 추가 후 Sentry **Envelope HTTP API로 직접 POST**. 전송 전 `redact()`로 컨텍스트 PII 마스킹, 전송 실패는 catch로 삼켜 요청 흐름 무영향. **@sentry/nextjs SDK를 쓰지 않은 이유**: 이 환경에서 npm install이 완료되지 않아(아래 참고) 의존성 추가분의 빌드 검증이 불가 → 신규 의존성 0 방식을 택해 Vercel 빌드 리스크를 제거. 트레이드오프는 자동 브레드크럼·퍼포먼스·소스맵 스택 복원 없음(수동 captureError 전송 전용)이며, 추후 SDK 승격 시 forwardToSink 본문만 교체하면 됨. **환경변수 미설정 시 동작은 종전과 동일한 완전 no-op**이므로 기본값은 여전히 OFF. — src/lib/logger.ts
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주** — `error TS` 0건. `node --test tests/*.test.ts` **89/89 통과**(신규 9건: redact PII 마스킹·대소문자 변형·newRequestId 재사용/거부·알림 기본OFF·requestContext 쿼리스트링 배제·x-request-id 전파·ApiError/pg코드 매핑 유지·req 미전달 레거시 동작). 작업 전 src 백업(/tmp/bak_1788311145). 임의 KPI 수치 미기재.
- ⚠️ **미검증 항목 · 로컬 node_modules 손상** — `next build` **완주 실패(환경 한계, 코드 문제 아님)**: 샌드박스의 OneDrive 마운트가 너무 느려 빌드가 165초 캡을 초과함(`du -sh node_modules`조차 120초 타임아웃). 따라서 이번 배치는 tsc·단위테스트까지만 검증됨. Vercel 빌드 breaker 2종은 수동 확인 완료(route.ts는 POST·dynamic 외 export 0건, global-error.tsx는 'use client'). 또한 @sentry/nextjs 설치 시도가 타임아웃으로 중단되면서 **로컬 node_modules 46개 패키지가 부분 손상**됨(npm 스테이징 디렉터리 `.name-HASH`가 승격되지 않음). 스크립트로 46개 전부 additive copy 승격해 모듈 해석은 복구했으나, **완전 복구를 위해 Windows에서 `npm install` 1회 실행 권장**. **package.json·package-lock.json은 백업과 바이트 동일(무변경)이고 node_modules는 gitignore 대상이므로 커밋·배포에는 영향 없음.**

## 2026-08-13 (배치 148 — 배포 대기, 알림·감사로그 스크린리더 라이브 안내 — 배치147 패턴 확장)
- ⑨ **알림(/notifications)·감사로그(/audit) 목록 스크린리더 라이브 안내** — 배치147이 ResourceView 공용 목록에 라이브 리전을 넣었지만, 자체 표를 렌더하는 두 화면(ResourceView 미사용)은 여전히 `role="status"`/`aria-busy` 0건이라 로딩 완료·검색/필터 결과 변화가 보조기술에 안내되지 않았음. 배치147과 동일한 3종 보강 — (1) 툴바에 sr-only `role="status"` 라이브 리전: 로딩 중 `{화면명} 목록을 불러오는 중`, 완료 시 `{화면명} n건 표시(전체 n건 중)`(nfmt 쉼표, 기존 카운트 span과 동일 정보). (2) 표 래퍼 `aria-busy={!loaded}`. (3) 스켈레톤 행 `aria-hidden`. globals.css `.sr-only` 유틸(배치147 신설) 재사용. 순수 마크업 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/notifications/page.tsx, src/app/audit/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786579478). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 221·155줄·파일 끝 완전성 정상, sr-only·aria-busy·스켈레톤 aria-hidden 각 1건씩 삽입 확인.

## 2026-08-12 (배치 147 — 배포 대기, 목록 스크린리더 라이브 안내 — role="status"·aria-busy·스켈레톤 숨김)
- ⑨ **목록 공통: 로딩·검색/필터 결과 변화 스크린리더 라이브 안내** — 배치130~146 접근성 스윕이 정렬·그룹·다이얼로그·포커스·Shell까지 커버했지만, 전 소스에 `aria-live`/`role="status"`/`aria-busy`가 0건이라(결제 버튼 1곳 제외) 목록 로딩 완료나 검색·필터로 결과 건수가 바뀌어도 보조기술에 아무 알림이 없었음(시각 사용자만 스켈레톤·카운트로 인지). 세 가지 보강 — (1) 툴바에 시각적 숨김 `role="status"` 라이브 리전: 로딩 중 `{목록명} 목록을 불러오는 중`, 완료 시 `{목록명} n건 표시(전체 n건 중)`(nfmt 쉼표, 기존 카운트 span과 동일 정보) — 검색어 입력·필터 변경 시 결과 건수가 polite로 낭독됨. (2) 표 래퍼에 `aria-busy={loading}`. (3) 스켈레톤 행 5개는 장식이므로 `aria-hidden`(빈 행 낭독 제거). globals.css에 표준 클립 패턴 `.sr-only` 유틸 신설(전 화면 재사용 가능). 순수 마크업/표시 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/components/ResourceView.tsx, src/app/globals.css
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786536302). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 562·443줄·파일 끝 완전성 정상, sr-only 1+1건·aria-busy 1건·role="status" 삽입 확인.

## 2026-08-12 (배치 146 — 배포 대기, Shell 드롭다운 접근성 — aria-expanded·Escape 닫기·포커스 복귀)
- ⑨ **Shell 공통: 상단바 드롭다운 3종(프로젝트 선택·알림·사용자 메뉴) 접근성 보강** — 배치131·145가 ResourceView의 오버레이를, 배치135까지가 페이지들을 커버했지만 전 화면이 공유하는 Shell 자체의 드롭다운은 미처리였음: 트리거 버튼에 `aria-haspopup`/`aria-expanded`가 없어 보조기술이 메뉴 열림을 인지 못 했고, Escape는 ⌘K 팔레트만 닫혀 드롭다운은 바깥 클릭 외 키보드 닫기 수단이 없었음. 세 트리거에 `aria-haspopup="menu"`+`aria-expanded` 부여, 전역 Escape 핸들러 확장(열린 드롭다운·모바일 사이드바 닫기) + 닫을 때 트리거 버튼으로 포커스 복귀(trigRef, 배치145 복귀 패턴 — 메뉴 항목에 포커스가 있던 경우 포커스 유실 방지). 모바일 메뉴 버튼에도 `aria-expanded={menuOpen}`. 덤으로 ⌘K 팔레트 컨테이너에 `role="dialog"`+`aria-modal`+`aria-label`(배치131 다이얼로그 시맨틱의 Shell 누락분). 순수 마크업/핸들러 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/Shell.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786471506). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 237줄·파일 끝 완전성 정상, trigRef 5건·aria-expanded 4건·aria-haspopup 3건·role="dialog" 1건 확인.

## 2026-08-10 (배치 145 — 배포 대기, 다이얼로그 포커스 관리 — 자동 포커스·Tab 가둠·복귀)
- ⑨ **목록 공통: 상세 슬라이드오버·수정/생성 모달 포커스 관리** — 같은 날 배치144까지 마무리된 접근성 스윕의 연장 — 배치131이 두 오버레이에 `role="dialog"`+`aria-modal="true"`를 붙이고 배치143이 폼 라벨-입력을 연결했지만, 정작 포커스는 오버레이 밖(뒤쪽 목록)에 남아 있었음: 모달을 열어도 첫 입력에 커서가 가지 않아 Tab을 여러 번 눌러야 했고, Tab을 계속 누르면 가려진 배경 목록의 링크·버튼으로 포커스가 새어나갔으며(aria-modal 선언과 모순), 닫은 뒤에는 포커스가 문서 처음으로 초기화돼 키보드 사용자가 위치를 잃었음. 세 가지를 한 번에 처리 — (1) **자동 포커스**: 열림 시 `setTimeout(0)`으로 렌더 후 모달은 첫 입력 컨트롤(INPUT/TEXTAREA/SELECT, 없으면 첫 포커스 요소), 상세 패널은 첫 포커스 가능 요소로 이동. (2) **Tab 순환 가둠**: 두 오버레이 컨테이너에 `onKeyDown={trapTab}` — 마지막 요소에서 Tab→첫 요소, 첫 요소에서 Shift+Tab→마지막 요소(숨김 요소는 `offsetParent` 기준 제외). (3) **포커스 복귀**: 열기 직전 `document.activeElement`를 ref에 저장했다가 닫힘(effect cleanup) 시 해당 요소가 아직 문서에 있으면 되돌림 — 목록 행에서 상세를 열고 닫으면 그 행으로 복귀. 이펙트 의존성은 `[open, detailOpen(=Boolean(detail))]`이라 ↑/↓ 레코드 이동(배치26·상세 유지)에는 재포커스가 걸리지 않도록 했고, Escape 닫기(기존 전역 핸들러)·스와이프 닫기·스크림 클릭 모두 동일 경로로 복귀. 순수 포커스/마크업 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json` **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_wk_1786324660). 동일 시각 배치144(서브컴포넌트 3종)와 파일 겹침 없음(ResourceView.tsx 단독) 확인 후 번호 145로 조정. 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 559줄·파일 끝 완전성 정상, trapTab/modalRef/overRef/lastFocusRef 각 3건(정의+적용) 확인.

## 2026-08-10 (배치 144 — 배포 대기, 상세 패널 서브컴포넌트 3종 카운트 쉼표 + 접근성 마감)
- ⑨ **상세 패널 서브컴포넌트(댓글·이슈 변경이력·산출물 버전이력) 일관화 마감** — 배치114~139 카운트 쉼표 스윕과 배치130~143 접근성 스윕이 모두 **페이지(page.tsx)와 ResourceView/Shell/views 3대 컴포넌트 기준**이었던 탓에, 상세 슬라이드오버 안에서 렌더되는 서브컴포넌트 3종이 두 스윕 모두에서 누락돼 있었음. (1) **카운트**: 섹션 헤더의 `댓글 (n)`·`변경 이력 (n)`·`버전 이력 (n)`, 관심 버튼의 워처 수가 원시 정수 → 각 파일에 `nfmt(n)=n.toLocaleString('ko-KR')` 추가(배치114~143과 동일 표기) + 세 헤더에 건수 hover 툴팁 신설(0건일 때는 빈 상태 사유 안내, 배치120 툴팁 패턴). (2) **접근성**: 댓글 등록 버튼이 **아이콘 전용인데 접근 이름이 없어** 스크린리더에 "버튼"으로만 낭독됐음 → `aria-label='댓글 등록'`+상황별 title, 아울러 `type='submit'` 명시와 빈 입력 시 `disabled`(기존 submit 핸들러가 이미 `!text.trim()`이면 무시하던 동작을 버튼 상태로 노출 — 설정 화면 저장 버튼과 동일 패턴)를 부여. 댓글 textarea에 `aria-label`(배치143 레이블 없는 컨트롤 스윕의 잔여 지점), 이슈 관심 버튼에 토글 상태 `aria-pressed`+워처 수 포함 aria-label, 장식용 아바타 이니셜 div·헤더 아이콘에 `aria-hidden`(중복 낭독 제거, 배치130~132 패턴). (3) 워처 줄은 `워처: 이름…` → `워처 n명: 이름…`+설명 툴팁으로 보강. 순수 표시/마크업 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 3파일. — src/components/Comments.tsx, src/components/IssueJournal.tsx, src/components/DocumentVersions.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(90s 캡) **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786324591). 마운트(=OneDrive 실파일) 무결성 확인 — 3파일 깨진문자(U+FFFD) 0, 38·71·31줄·파일 끝 완전성 정상, nfmt 2·5·2건(정의 포함)·aria 속성 3·4·2건 확인.
- 참고: 사전 재스캔에서 /settings 등 나머지 화면의 `onClick`은 전부 네이티브 `<button>`(키보드 기본 지원)임을 확인 — 마우스 전용 잔여는 여전히 로그인(/login) 탭 div 2개뿐(인증 화면, 주간 수동).

## 2026-08-08 (배치 143 — 배포 대기, 폼 라벨-입력 연결 + 툴바 레이블 없는 컨트롤 aria-label)
- ⑨ **목록 공통: 수정/생성 모달 폼 라벨-입력 연결(htmlFor/id) + 툴바 레이블 없는 컨트롤 스윕** — 배치130~142 접근성 스윕이 정렬·그룹·다이얼로그·클릭 요소·skip link를 커버했지만, 정작 폼 내부는 `<label>{f.label}</label>`이 입력과 미연결이라 스크린리더가 모든 입력을 "편집 가능"으로만 낭독했고 라벨 클릭 포커스도 불가였음. 각 필드에 `id='ff-'+f.key` 부여 + `htmlFor` 연결(textarea·select·combo·comma·기본 input 5개 분기 전부), 필수 필드(`f.required`)는 `aria-required`로 노출(네이티브 required는 기존 검증 흐름 변경 우려로 미사용), 저장 오류 배너에 `role="alert"`(오류 발생 즉시 낭독). 툴바도 레이블 없는 컨트롤 4종을 보강: 검색 입력 `aria-label={title} 검색`·검색어 지우기 버튼(aria-label+title)·상태 필터/그룹화 select aria-label·하단 빠른 추가 입력 aria-label. 순수 마크업 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(55s 캡) **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_wk_1786168944). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 521줄·파일 끝 완전성 정상, htmlFor 1건·id/aria-required 각 5건·role="alert" 1건·aria-label 신규 5건 확인.

## 2026-08-08 (배치 142 — 배포 대기, 본문 바로가기 skip link + main 랜드마크)
- ⑨ **전 화면 공통: '본문으로 건너뛰기'(skip to content) 링크 + `<main>` 랜드마크** — 배치130~135·141 접근성 스윕이 정렬·그룹·다이얼로그·대체뷰·클릭 요소를 커버했지만, 키보드 사용자는 매 화면 진입 시 사이드바 링크 약 29개+상단바를 전부 Tab으로 지나야 본문에 도달했고(WCAG 2.4.1 Bypass Blocks 미충족), 본문 영역이 `div.content`라 `<main>` 랜드마크도 부재였음. Shell 최상단에 `<a href="#main-content" class="skip-link">본문으로 건너뛰기</a>` 추가 — 평소 화면 밖(top:-52px) 절대배치라 시각 사용자에게 무영향, Tab 첫 포커스 시 브랜드색 배너로 노출(`:focus`→top:0, 배치130 계열과 동일한 순수 마크업 접근). 본문은 `div.content`→`<main id="main-content" tabIndex={-1} class="content">`로 교체해 앵커 이동 시 포커스 착지+보조기술 main 랜드마크 제공(globals.css 셀렉터가 전부 `.content` 클래스 기반이라 스타일 무영향 확인). `.app`은 flex지만 skip-link는 absolute라 레이아웃 무영향. Shell 공통이라 22개 화면+전 목록 일괄 커버. 순수 마크업/CSS — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/components/Shell.tsx, src/app/globals.css
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(50s 캡) **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_wk_1786154541). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 228·439줄·파일 끝 완전성 정상, skip-link 2건(마크업+CSS)·#main-content 3건 확인.

## 2026-08-08 (배치 141 — 배포 대기, Shell 공통 알림 카운트 쉼표 + 사이드바 aria-current)
- ⑨ **Shell 공통: 알림 unread 카운트 천단위 쉼표 + 사이드바 현재 화면 aria-current** — 배치123~139 카운트 쉼표 스윕이 화면(page) 단위였던 탓에 전 화면이 공유하는 Shell.tsx 자체가 누락돼 있었음: 사이드바 nav-badge·알림 드롭다운 헤더 `(n)`·사용자 메뉴 알림 배지·모바일 탭 mt-badge 4지점이 원시 정수. 컴포넌트 밖 `nfmt(n)=n.toLocaleString('ko-KR')` 추가 후 일괄 적용(배치114~139와 동일 표기) + 사이드바·사용자 메뉴 배지에 `안 읽은 알림 n건` hover 툴팁, 상단 벨 버튼 aria-label에 안읽음 건수 포함(`알림 (안 읽음 n건)`). 또한 모바일 탭바는 `aria-current="page"`를 노출하는데 사이드바 nav-item은 미노출이라 스크린리더가 현재 화면을 인지 못 했음 — 동일 패턴으로 부여(active 계산식 재사용, 표시 무변경). 순수 표시/마크업 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/Shell.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(50s 캡) **완주** — rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786125900). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 227줄·파일 끝 완전성 정상, nfmt 7건(정의 포함)·aria-current 2건 확인.

## 2026-08-07 (배치 140 — 배포 대기, 브라우저 탭 제목 화면별 동기화)
- ⑨ **브라우저 탭 제목을 현재 화면명으로 동기화(`{화면명} — PMS`)** — SPA 특성상 모든 화면의 탭 제목이 layout 메타데이터의 "PMS — Project Management System"으로 고정돼 있어, 탭 여러 개를 띄우면 구분 불가·북마크/히스토리 이름 무의미·스크린리더 화면 전환 미인지 문제가 있었음. 전 화면이 공유하는 Shell이 이미 `title` prop(한글 화면명, 프로젝트 상세는 `코드 · 이름`)을 받고 있어, `useEffect(document.title = title + ' — PMS', [title])` 성격의 이펙트 1줄로 22개 Shell 페이지 + 전 ResourceView 목록을 일괄 커버. 접미사는 인앱 브랜드(Logo·랜딩 title)와 동일한 "PMS" 사용 — "PRISM" 표기 통일 여부는 브랜딩 결정이라 주간 판단 사항으로 남김. 비-Shell 화면(랜딩·로그인·요금제 등)은 기존 메타데이터 유지. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/Shell.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(44s 캡) **완주** — tsc rc=0·`error TS` 0건·출력 0줄. 작업 전 src 백업(/tmp/bak_1786104308). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 227줄·파일 끝 완전성 정상, title 이펙트 삽입 확인.

## 2026-08-07 (배치 139 — 배포 대기, 잔여 7개 화면 카운트 천단위 쉼표 — 배치123 스윕 최종 완료)
- ⑨ **요구사항·회의·프로젝트·리스크·산출물양식·테스트·방화벽 카운트·툴팁 천단위 쉼표 통일** — 배치138 재스캔 메모의 "다음 배치 후보" 7개 화면을 일괄 처리해 카운트 쉼표 스윕을 전 화면 완료. 요구사항: 분포 Bar 값·`전체/승인 n건`·연계 칩(업무·이슈·테스트)+툴팁·커버리지 툴팁. 회의: 참석자 `n명` 칩·툴팁·`외 n`, 안건·결정·조치 칩/툴팁. 프로젝트: 업무 셀 `완료/총`·`⚠n`(기한초과)·`⚠ 잔여 n`·툴팁 2종. 리스크: 완화·비상 pill+툴팁. 산출물양식: 항목 `n개` 칩·툴팁·적용 산출물 `승인/총`·`⚠n`(반려)+툴팁. 테스트: 절차·기대 칩/툴팁. 방화벽: 중복 신청 툴팁 `n건`·허용 포트 `n개` 툴팁(kr 로케일 통일). 각 파일에 `nfmt(n)=n.toLocaleString('ko-KR')` 추가, 배치114·120~138과 동일 표기. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 7파일(각 3~6지점 소규모 기계적 치환이라 3파일 권장을 초과하되 저위험으로 판단). — src/app/{requirements,meetings,projects,risks,form-definitions,tests,firewall}/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — rc=0·`error TS` 0건. 작업 전 src 백업(/tmp/bak_1786039472). 마운트(=OneDrive 실파일) 무결성 확인 — 7파일 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상(145·78·123·96·86·105·155줄), nfmt 17·14·12·6·13·7·4건(정의 포함) 확인.
- 참고: 이로써 카운트 천단위 쉼표 스윕(배치114 시작)은 전 소스 완료. ROADMAP ⑨ 메모 갱신.

## 2026-08-06 (배치 138 — 배포 대기, 단계·인력·테스트차수 연계 집계 카운트 천단위 쉼표 — 배치123 스윕 잔여)
- ⑨ **단계(/phases)·인력(/members)·테스트 차수(/test-cycles) 연계 집계 카운트 천단위 쉼표 통일** — 배치123~137 스윕 후 재스캔 결과 도메인 관리 3개 화면의 행 연계 집계(보조 fetch 기반 읽기 전용 렌더)가 원시 정수였음. 단계: 업무 셀 `완료/총`·`⚠ 잔여 n`·`⚠n`(기한초과)·툴팁 4종·진척 툴팁 `업무 n건`. 인력: 업무 셀 `완료/총`·`⚠n`·부하 `n건`·미해결 이슈 `n건`·툴팁 3종. 테스트차수: 케이스 셀 `실행/총`·`⚠ 미실행 n`·통과율 `⚠n`(실패)·`■n`(블록)·툴팁 4종. 각 파일에 `nfmt(n)=n.toLocaleString('ko-KR')` 추가, 배치114·120~137과 동일 표기. `n줄` 배지는 배치129 방침대로 제외. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 3파일. — src/app/phases/page.tsx, src/app/members/page.tsx, src/app/test-cycles/page.tsx
- 참고: 카운트 쉼표 잔여 화면 스캔 메모 — requirements(5)·meetings(5)·projects(4)·risks(3)·form-definitions(3)·tests(1)·firewall(1) 지점 잔존, ROADMAP에 다음 배치 후보로 기록.
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(44s 캡) **완주** — rc=0·`error TS` 0건. 작업 전 src 백업(/tmp/bak_1786017871). 마운트(=OneDrive 실파일) 무결성 확인 — 3파일 깨진문자(U+FFFD) 0, 88·129·140줄·파일 끝 완전성 정상, nfmt 14·16·17건(정의 포함) 확인.

## 2026-08-06 (배치 137 — 배포 대기, 관리자 화면 2종 카운트 천단위 쉼표 — 배치123 스윕 admin 누락분)
- ⑨ **사용자·권한(/admin)·보안 이벤트(/admin/security) 카운트 천단위 쉼표 통일** — 배치123~136 스윕이 일반 화면을 모두 커버했지만 재스캔 결과 admin 하위 2개 화면이 누락돼 있었음. /admin: KPI 4종(전체 구성원·활성·비활성·관리자)·`활성 n명` 부제·역할 구성 칩 `역할 n`(+툴팁 `n명`)·스키마 업데이트 결과 `완료 · n개 적용됨`이 원시 정수였음. /admin/security: 로그 화면이라 건수가 커질 수 있는데 `표시 n건 · 로그인 실패 n건`이 원시 정수였음 — nfmt 적용 + 불러온 범위 안내 hover 툴팁 신설(`현재까지 불러온 이벤트 n건 — 더 보기로 추가 로드 / (전부 불러옴)`, 배치120 툴팁 패턴). 각 파일에 `nfmt(n)=n.toLocaleString('ko-KR')` 추가, 배치121~136과 동일 표기. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/admin/page.tsx, src/app/admin/security/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — rc=0·`error TS` 0건. 작업 전 src 백업(/tmp/bak_1785953119). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 75·82줄·파일 끝 완전성 정상, nfmt 9·5건(정의 포함) 확인.

## 2026-08-05 (배치 136 — 배포 대기, 리포트·워크로드 표 셀 카운트 천단위 쉼표 — 배치123 스윕 잔여)
- ⑨ **리포트(/reports)·워크로드(/workload) 잔여 원시 정수 카운트 천단위 쉼표 통일** — 배치123이 두 화면의 툴팁·부제·합계를 nfmt로 맞췄지만 재스캔 결과 표 셀·차트 라벨이 남아 있었음. 리포트: 번다운 SVG `완료 n/n` 라벨, KPI Stat 3종(열린 이슈·리스크·참여 인원), Bars 차트 값 배지(이슈 우선순위·테스트 결과 분포 공용), 담당자별 이슈 표 셀 4종, 벨로시티 `이슈·pts`, 테스트 `실행 n/n`(+실행 기준 툴팁 신설), 프로젝트별 테스트 `실행/전체·통과·실패` 셀. 워크로드: 표 셀 4종(진행 업무·미결 이슈·완료·부하 — 툴팁만 nfmt이고 셀은 원시 정수였음). 덤으로 리포트 KPI `High 리스크` 라벨을 `LABEL.high`(높음) 기반으로 교체(배치127 대시보드 한글화의 잔여 지점). 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/reports/page.tsx, src/app/workload/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — rc=0·`error TS` 0건. 작업 전 src 백업(/tmp/bak_1785931528). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 309·95줄·파일 끝 완전성 정상, nfmt 32·19건(정의 포함) 확인.

## 2026-08-05 (배치 135 — 배포 대기, 알림 그룹 헤더 키보드 접근성 — 배치131 패턴 마지막 누락 지점)
- ⑨ **알림(/notifications) 그룹 헤더 키보드 접기/펼치기** — 배치131이 ResourceView 그룹 헤더를, 배치133이 대시보드·내작업·통합캘린더를 커버한 뒤 전 소스 재스캔 결과, 알림의 종류별 그룹 헤더 `<tr onClick>`만 마우스 전용으로 남아 있었음(알림 행 자체는 기존 처리됨). `tabIndex={0}`+Enter/Space→setCollapsed(preventDefault)+`aria-expanded`+`aria-label`(그룹명·동작)+툴팁 동작 안내, 접기 화살표(▸/▾)는 `aria-hidden` — 배치131과 동일 패턴. 덤으로 '안 읽음만' 버튼 카운트 `(${unread})`가 원시 정수였던 것을 기존 `nfmt`로 쉼표 통일(같은 파일 markAll 툴팁은 이미 nfmt 사용 중이라 표기 일치). 순수 마크업/표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/app/notifications/page.tsx
- 참고: 이로써 전 소스 grep 기준 마우스 전용 클릭 요소는 로그인(/login) 탭 div 2개만 남음 — 인증 화면이라 야간 금지 규칙에 따라 미수정, ROADMAP에 주간 수동 메모.
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785866710). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 220줄·파일 끝 완전성 정상, aria-expanded 1건·nfmt(unread) 적용 확인.

## 2026-08-03 (배치 134 — 배포 대기, RTM 연계 칩 클릭 이동 링크 + 툴팁 한글 라벨화)
- ⑨ **RTM(/rtm) 연계 칩(업무·이슈·테스트)을 클릭 이동 링크로 + 툴팁 원시 영문 라벨 제거** — 상세 슬라이드오버는 배치115부터 연계 값이 `?q=` 검색 복원 링크인데, RTM 매트릭스의 연계 칩은 여전히 정적 `<span>`이라 코드를 눈으로 외워 이동해야 했고, 테스트 칩 툴팁은 전 소스에서 유일하게 원시 영문 결과값(`· pass`/`· fail`)을 노출했음(배치126·127 라벨 스윕 잔여). 세 칩을 `<a href="/{tasks|issues|tests}?q=<code>">`로 교체(URL 쿼리 검색 복원=배치113·115 패턴, `<a>`라 키보드 포커스 기본 제공) — 기존 칩 색상 유지(+textDecoration:none). 툴팁은 `이름 · 상태/결과 한글(LABEL 폴백)` + 클릭 안내로 보강(테스트 결과 없음은 '미실행'). LABEL import 1건 추가. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/app/rtm/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — rc=0·`error TS` 0건. 작업 전 src 백업(/tmp/bak_1785758694). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 113줄·파일 끝 완전성 정상, 링크 3건·LABEL 적용 3건 삽입 확인.

## 2026-08-03 (배치 133 — 배포 대기, 대시보드·내작업·통합캘린더 클릭 요소 키보드 접근성 — 잔여 스윕)
- ⑨ **대시보드·내작업·통합캘린더 키보드 접근성 보강 — 배치130~132가 ResourceView(목록)·대체뷰(views.tsx)를 커버한 뒤 전 소스 스캔 결과, 마우스 전용 클릭 요소가 3개 화면에 남아 있었음**(알림 행은 기존에 이미 처리됨을 확인·제외). (1) **대시보드** 5곳: 내 작업 항목·마감임박/결재대기 행·내 To-Do 행·담당자 부하 행(div onClick)에 `role="button"`+`tabIndex={0}`+Enter/Space→router.push+`aria-label`(식별자+이동 대상), 최근 프로젝트 `<tr onClick>`에 tabIndex+onKeyDown+aria-label(+cursor:pointer). (2) **내 작업(/mywork)** 섹션 행 `<tr onClick>`: 동일 패턴(code/name/title 폴백 식별자). (3) **통합 캘린더(/calendar)** 일정 칩(div onClick): role="button"+tabIndex+Enter/Space+aria-label(종류·기한초과 여부 포함) — views.tsx CalendarView는 배치132에서 처리됐으나 /calendar 페이지 자체 칩은 별도 구현이라 누락돼 있었음. 각 파일에 공통 헬퍼 `onKeyAct`(Enter/Space·preventDefault) 추가 — 전역 `[tabindex]:focus-visible` 링 자동 적용. 순수 마크업/핸들러 추가라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 3파일. — src/app/dashboard/page.tsx, src/app/mywork/page.tsx, src/app/calendar/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(44s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_wk_1785717094). 마운트(=OneDrive 실파일) 무결성 확인 — 3파일 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상(240·114·141줄), onKeyAct 6·2·2건(정의+적용) 삽입 확인.

## 2026-08-03 (배치 132 — 배포 대기, 대체뷰 키보드 접근성 — 칸반·리스크매트릭스·간트·캘린더)
- ⑨ **대체뷰(views.tsx) 키보드 접근성 보강 — 배치130·131이 ResourceView(목록)의 정렬 헤더·그룹 헤더·다이얼로그를 키보드 접근 가능하게 만든 반면, 대체뷰 4종은 aria 속성 0건·마우스 전용이었음** — (1) **칸반 카드**(`.kb-card` div onClick): `role="button"`+`tabIndex={0}`+Enter/Space→openDetail+`aria-label`(`{code} {title} 상세 열기`). (2) **리스크매트릭스 셀**(td onClick): 리스크가 배치된 셀에만 조건부 스프레드로 role/tabIndex/onKeyDown/`aria-label`(발생가능성×영향도·건수) 부여 — 빈 셀은 포커스 대상에서 제외. (3) **간트 단계 스윔레인 헤더**(접기/펼치기 div): tabIndex+Enter/Space toggle+`aria-expanded`+aria-label, 회전 화살표(▾)는 `aria-hidden`. (4) **간트 작업 라벨**·(5) **캘린더 일정 칩**: role="button"+tabIndex+Enter/Space→openDetail+aria-label. 공통 헬퍼 `onKeyAct`(Enter/Space·preventDefault) 1개 추가 — 전역 `[tabindex]:focus-visible` 링(v20)이 자동 적용. 순수 마크업/핸들러 추가라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/views.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785715418). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 472줄·파일 끝 완전성 정상, onKeyAct 6건(정의1+적용5)·role="button" 5건·aria-label 5건 삽입 확인.

## 2026-08-02 (배치 131 — 배포 대기, 목록 접근성 잔여 보강 — 그룹 헤더 키보드·체크박스 라벨·다이얼로그 시맨틱)
- ⑨ **목록 공통: 배치130(정렬 헤더) 이후 남은 접근성 사각지대 4곳 보강** — (1) **그룹화 헤더 행**이 마우스 전용(`<tr onClick>`)이라 키보드로 접기/펼치기 불가·펼침 상태 미노출이었음 → `tabIndex={0}`(전역 focus-visible 링 자동 적용)+Enter/Space `toggleGroup` 호출(스크롤 방지 preventDefault)+`aria-expanded`+동작 안내 hover 툴팁 추가, 회전 화살표(▸)는 `aria-hidden`으로 중복 낭독 제거 — 배치130 정렬 헤더와 동일 패턴. (2) **행 선택 체크박스**가 레이블 없는 컨트롤(스크린리더에 "체크박스"로만 낭독) → `aria-label`에 행 식별자(code/title/name/#id)+"선택" 부여(전체 선택 체크박스는 기존 라벨 유지). (3) **상세 슬라이드오버**(`aside.over`)와 (4) **수정/생성 모달**(`form.modal`)에 다이얼로그 시맨틱이 없어 보조기술이 오버레이 진입을 인지 못 했음 → `role="dialog"`+`aria-modal="true"`+`aria-label`(`{title} 상세` / `{title} 수정·새로 만들기`) 부여. 정렬·그룹·선택·저장 로직 무변경, 순수 마크업/핸들러 추가라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 스코프 tsconfig(`tsconfig.check.json`)도 0건 교차확인. 작업 전 src 백업(/tmp/bak_1785632328). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 518줄·파일 끝 완전성 정상, aria-expanded·role="dialog"·aria-modal 각 삽입 확인.

## 2026-08-01 (배치 130 — 배포 대기, 목록 정렬 헤더 키보드 접근성)
- ⑨ **목록 공통: 정렬 컬럼 헤더 키보드 접근성 보강** — ④의 접근성 하드닝(전역 focus-visible 링·aria-label)이 버튼·입력에는 적용됐지만, 전 목록이 공유하는 ResourceView의 **정렬 헤더(`<th onClick>`)는 마우스 전용**이었음: Tab 포커스 불가, Enter/Space로 정렬 불가, 현재 정렬 상태(`aria-sort`)·헤더 역할(`scope`) 미노출이라 키보드·스크린리더 사용자는 정렬 기능 자체를 쓸 수 없었음. 헤더에 `scope="col"`+`tabIndex={0}`(전역 `[tabindex]:focus-visible` 링이 자동 적용)+`onKeyDown`(Enter/Space→기존 `sort(c.key)` 호출·스크롤 방지 preventDefault)+`aria-sort`(ascending/descending/none)+정렬 안내 hover 툴팁을 추가하고, 정렬 화살표(▲/▼)는 `aria-hidden`으로 중복 낭독 제거. 체크박스·행 작업 열 `<th>`에도 `scope="col"` 부여. 정렬 로직·상태 저장(localStorage)·URL 쿼리(배치113) 등 기존 동작 무변경, 순수 마크업/핸들러 추가라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_wk_1785575446). 마운트(=OneDrive 실파일) 무결성 확인 — 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상(517줄), aria-sort·Enter/Space 핸들러 삽입 확인.

## 2026-08-01 (배치 129 — 배포 대기, 백로그·감사로그 카운트 천단위 쉼표 — 스윕 잔여 2개 화면)
- ⑨ **백로그(/backlog)·감사로그(/audit) 카운트 숫자 천단위 쉼표 통일** — 배치123이 "카운트 쉼표 전 화면 스윕 완료"로 기록했지만 전 소스 재스캔 결과 두 화면이 누락돼 있었음. 백로그: KPI 5종(전체 이슈·백로그 대기·스프린트·잔여 포인트·미추정)과 pts 부제, 스프린트/백로그 헤더 `n건 · n pts`, 미추정 칩·진척 툴팁(`총/완료/남은`·`dp/tp pts`)이 원시 정수였음(19지점). 감사로그: 동작·영역 필터 옵션 건수, `오늘 n건`, 툴바 `표시/전체건`·툴팁이 원시 정수였음(15지점). 각 파일 상단에 `nfmt(n)=n.toLocaleString('ko-KR')`를 두고 적용 — 배치120~128과 동일 표기. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/backlog/page.tsx, src/app/audit/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785521132). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상, nfmt 19·15건 삽입 확인.
- 참고: 게시판(boards)·기성고(snapshots)의 다중행 `n줄` 배지는 항상 한 자리 수준이라 이번 스윕에서 제외(의도적).

## 2026-07-31 (배치 128 — 배포 대기, 통합 캘린더 카운트 쉼표·빈 상태 + 대체뷰 전체 빈 상태 문구)
- ⑨ **통합 캘린더(/calendar) 카운트 쉼표 + 일정 전무 빈 상태** — 배치123이 "카운트 쉼표 전 화면 스윕 완료"로 기록했지만 /calendar 페이지가 스윕 목록에서 누락돼 있었음: 기한 초과 배지 `⚠ 기한 초과 n건`(+툴팁), 종류 필터 칩 건수(회의·업무마감·테스트기한·이슈기한), 셀 넘침 `+n`이 원시 정수였음. `nfmt(n)=n.toLocaleString('ko-KR')`를 두고 4개 지점에 적용(배치121~123과 동일 표기). 또한 프로젝트 선택 후 일정이 0건이면 빈 월력 격자만 보였음 — `등록된 일정이 없습니다. 회의·업무 마감·테스트/이슈 기한을 입력하면 월별 캘린더에 표시됩니다.` 빈 상태 카드 추가(기존 '프로젝트 미선택' 안내와 동일 톤).
- ⑨ **대체뷰(칸반·리스크매트릭스·CalendarView) 전체 빈 상태 문구** — views.tsx에서 간트만 0건 빈 상태 안내가 있고, 칸반은 빈 컬럼마다 '없음'만 나열, 리스크매트릭스는 0건 격자, CalendarView는 빈 월력을 그대로 렌더했음. 세 컴포넌트에 간트와 동일 톤의 `rows.length === 0` 빈 상태 카드를 추가(칸반: 새로 만들기→카드 안내 / 리스크: 발생가능성·영향도 입력→매트릭스 배치 안내 / 캘린더: 날짜 입력 항목 표시 안내). CalendarView는 useState 훅 뒤에 배치해 훅 규칙 준수(칸반·매트릭스는 훅 없음). 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/calendar/page.tsx, src/components/views.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785499520). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상, nfmt 4건·빈 상태 return 4건(views.tsx) 삽입 확인.

## 2026-07-31 (배치 127 — 배포 대기, 대시보드·리포트 분포 막대 라벨 한글화 + 대시보드 KPI 부제 카운트 쉼표)
- ⑨ **대시보드·리포트 분포 막대 라벨 한글화(배치126 잔여 스윕)** — 배치126이 프로젝트 상세의 Bar 라벨을 `LABEL`로 한글화했지만, 전 소스 스캔 결과 같은 영문 리터럴 하드코딩이 2곳 남아 있었음: 대시보드 리스크 등급 분포('High'·'Medium'·'Low')와 리포트 이슈 우선순위 분포('Critical'·'High'·'Medium'·'Low'). 두 파일에 `LABEL` import 추가 후 7개 라벨을 `LABEL.high`(높음) 등으로 교체 — 배지·필터·그룹헤더(배치119)·프로젝트 상세(배치126)와 표기 일치. 집계 키(cnt의 원시 코드값)는 유지, 표시만 변경.
- ⑨ **대시보드 KPI 카드 부제 카운트 쉼표 + 'High' 한글화** — KPI 카드 부제(`진행 n`·`승인 n`·`전체 n`·`High n`·`완료 n`)가 원시 정수·영문 혼용이었음. 기존 `nfmt`로 천단위 쉼표 적용(배치123 스윕 누락분), `High`→`LABEL.high`(높음). 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/dashboard/page.tsx, src/app/reports/page.tsx
- 검증: 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0, 출력 0줄). 작업 전 src 백업(/tmp/bak_1785434709). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, 파일 끝 완전성 정상, LABEL 적용 삽입 확인.
- 참고: `src/src/` 하위에 구버전 소스 사본이 남아 있어 tsc 검사 범위에 포함되고 있음(현재 에러 0이라 무해). 주간에 의도 확인 후 삭제 권장(야간 삭제는 보류).

## 2026-07-30 (배치 126 — 배포 대기, 프로젝트 상세 분포 라벨 한글화 + 대체뷰 잔여 카운트 쉼표·툴팁)
- ⑨ **프로젝트 상세(/projects/[id]) 이슈·리스크 분포 막대 라벨 한글화** — 요구사항 분포 분석(requirements)은 `LABEL.high` 등 한글(긴급·높음·보통·낮음)로 표기하는데, 프로젝트 상세의 같은 Bar 컴포넌트만 영문 리터럴('Critical'·'High'·'Medium'·'Low')을 하드코딩해 배지·필터·그룹헤더(배치119)와 어긋났음. `LABEL` import 추가 후 7개 라벨을 `LABEL.critical` 등으로 교체(집계 키는 원시 코드값 유지, 표시만 변경).
- ⑨ **대체뷰 잔여 카운트 쉼표·툴팁 보강(배치122 스윕 누락분)** — views.tsx에서 간트 단계 스윔레인 헤더의 작업 수 `{gr.length}`와 캘린더 셀 넘침 표시 `+n`이 원시 정수였음. 기존 `nfmt`로 천단위 쉼표 적용 + hover 툴팁(단계 작업 건수 / 미표시 일정 안내) 추가. 순수 표시 로직 — 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 2파일. — src/app/projects/[id]/page.tsx, src/components/views.tsx
- 검증: 호스트 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785413108). 마운트(=OneDrive 실파일) 무결성 확인 — 2파일 깨진문자(U+FFFD) 0, LABEL.critical 1건·nfmt(gr.length) 2건 삽입 확인.

## 2026-07-30 (배치 125 — 배포 대기, 요금제 페이지 결제 스캐폴딩 연결)
- ③④ **요금제(/pricing) 페이지에 결제 스캐폴딩 테스트 버튼 연결** — Basic·Pro 카드에 `결제 연동 테스트 (실결제 없음)` 버튼(점선·저채도, PAYMENTS_LIVE=false일 때만 노출) 추가. 클릭 시 `POST /api/billing/checkout`(배치124)를 호출해 테스트 모드 결제 파라미터 발급 흐름을 화면에서 검증(성공: paymentId 표시, 401: 로그인 안내, 오류: 표준에러 메시지). 신규 클라이언트 컴포넌트 `CheckoutButton.tsx` 1개 + pricing/page.tsx 2줄. Enterprise는 미노출(도입 문의 경로 유지). 실PG SDK 결제창·라이브 승격은 [승인 필요].
- 검증: 스코프 tsconfig(pricing 2파일+전이 의존성) `error TS` 0건(rc=0). 파일 끝 완전성·U+FFFD 0 확인.

## 2026-07-30 (배치 124 — 배포 대기, P0 포트원 구독 결제 스캐폴딩 + 구조화 로깅/에러캡처)
- ③ **포트원(PortOne) 구독 결제 스캐폴딩(P0) — build now, activate on approval** — (1) `src/lib/portone.ts` 신규: 테스트키 기반 설정 단일 소스(PORTONE), 결제상태 점검 `billingStatus()`, 멱등 결제ID `newPaymentId()`, 웹훅 HMAC-SHA256 서명검증 `verifyWebhook()`(시크릿 미설정 시 우회 사실 리턴). (2) API 3종 신규: `POST /api/billing/checkout`(인증+rate limit, 테스트 모드 결제 파라미터 발급, Enterprise 직접결제 차단, **PAYMENTS_LIVE=true면 오히려 차단**하는 실결제 방지 안전장치), `GET /api/billing/plans`(공개 요금제+결제상태), `POST /api/billing/webhook`(서명검증→파싱→로깅 후 항상 ack; 실구독 반영은 [승인 필요] 주석 지점). 시크릿은 응답에 절대 미포함. 실결제·라이브 승격은 **[승인 필요]**.
- ⑤ **구조화 로깅·에러 캡처 단일 소스** — `src/lib/logger.ts` 신규: 한 줄 JSON 로그(`formatLine` 순수함수·테스트 대상), LOG_LEVEL 임계, `captureError()`(스택 8줄 캡). Sentry 실전송은 MONITORING_ENABLED+SENTRY_DSN 시에만 훅되는 no-op 스텁(의존성 없음) — 실연동 [승인 필요]. instrumentation.ts의 console.error 2곳을 captureError로 교체. package.json에 `npm test` 스크립트 추가.
- 검증: 변경 파일 스코프 tsconfig 타입검증 `error TS` 0건(rc=0) — 샌드박스 환경 45s 캡으로 전체 tsc 완주 불가(변경 15파일+전이 의존성 검증으로 대체). 단위테스트 `node --test` 20/20 통과(billing·logger·ratelimit·http). 파일 끝 완전성·깨진문자(U+FFFD) 0 확인.

## 2026-07-30 (배치 123 — 배포 대기, 대시보드·리포트·워크로드·RTM·주간보고 잔여 카운트 천단위 쉼표 통일 — 전 화면 스윕 완료)
- ⑨ **대시보드·리포트·워크로드·RTM·주간보고의 잔여 카운트 숫자를 천단위 쉼표로 통일(카운트 쉼표 스윕 마무리)** — 배치120~122가 ResourceView·대체뷰·알림·내작업의 카운트를 `toLocaleString('ko-KR')`로 맞췄지만, 전 소스 스캔 결과 5개 화면에 원시 정수 카운트가 남아 있었음: (1) 대시보드 — 내작업/마감임박/내To-Do 위젯 `외 n건`, 담당자 부하 위젯 `진행 n`·`완료 d/t`(+툴팁), (2) 워크로드 — KPI(집계 인원·과부하·여유), 툴바 `n명`·`표시/전체명`(+툴팁), 완료·부하 툴팁, (3) RTM — 커버리지 요약 칩 건수·`n건 표시 (전체 n건)`, (4) 주간보고 — 상단 KPI 6종(이번 주 완료·누적 완료·진행중·지연·미결 이슈·High 리스크, +툴팁)·섹션 `n건`·누적/신규 note, (5) 리포트 — 조달 툴팁 `조달 n건`, 재무 합계 `(n개)`, 프로젝트별 업무 `d/t`·잔여(+툴팁 3종). 각 파일 상단에 `nfmt(n)=n.toLocaleString('ko-KR')`를 두고 위 지점에 적용, 배치114·118·120~122와 동일 표기로 통일. 이로써 grep 기준 카운트 원시 정수 노출 화면 스윕 완료. 순수 표시 로직이라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 5파일. — src/app/{dashboard,workload,rtm,weekly,reports}/page.tsx
- 검증: 호스트 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0). 작업 전 src 백업(/tmp/bak_1785348312). 마운트(=OneDrive 실파일) 무결성 재확인 — 5파일 깨진문자(U+FFFD) 0, nfmt 적용 9·15·3·17·10건 확인.


## 2026-07-29 (배치 122 — 배포 대기, 대체뷰·알림·내작업 카운트 숫자 천단위 쉼표 통일)
- ⑨ **대체뷰(칸반·리스크매트릭스·간트·캘린더)·알림·내작업의 카운트 숫자를 천단위 쉼표로 통일** — 배치120·121이 ResourceView(목록 툴바·그룹 헤더·선택 바·상세 인디케이터)의 카운트를 `toLocaleString('ko-KR')`로 맞췄지만, 같은 규칙이 적용돼야 할 나머지 화면은 여전히 원시 정수였음: (1) views.tsx — 칸반 컬럼 건수·지연 배지(+툴팁), 리스크매트릭스 `총 n건`·`미표시 n건`·심각도 범례 건수(+툴팁), 간트 `주경로 n개`·`지연 n` 배지(+툴팁), 캘린더 `이번 달 n건`, (2) 알림(/notifications) — `n건 읽지 않음`·모두읽음 툴팁·유형 필터 4종 건수·툴바 `표시/전체건`(+툴팁)·그룹 헤더 `n건`·`안 읽음 n`, (3) 내 작업(/mywork) — 섹션 `n건`·`지연 n건` 배지(+툴팁). 각 파일 상단에 카운트 포매터 `nfmt(n)=n.toLocaleString('ko-KR')`를 두고 위 지점에 적용해 배치114·118·120·121과 동일한 천단위 표기로 통일(매트릭스 셀 내부 숫자·정적 문구는 제외). 순수 표시 로직이라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 3파일. — src/components/views.tsx, src/app/notifications/page.tsx, src/app/mywork/page.tsx
- 검증: 호스트 전체 `tsc --noEmit -p tsconfig.json`(43s 캡) **완주** — `error TS` 0건(rc=0, 출력 0줄). 작업 전 src 백업(/tmp/bak_1785326722). 마운트(=OneDrive 실파일) 무결성 재확인 — 3파일 깨진문자(U+FFFD) 0, `nfmt` 적용 12·17·3건 삽입 확인.


## 2026-07-29 (배치 121 — 배포 대기, 나머지 카운트 숫자 천단위 쉼표 통일)
- ⑨ **목록/상세: 배치120에서 놓친 카운트 숫자들을 천단위 쉼표로 통일** — 배치120이 툴바 항목 카운트(`n건`·`표시/전체건`)를 `toLocaleString('ko-KR')`로 포매팅했지만, 같은 화면의 나머지 카운트 숫자는 여전히 원시 정수로 노출돼 있었음: (1) 다중선택 바 `n건 선택`·일괄삭제 확인창 `n건을 삭제`, (2) 그룹화 헤더 항목수 `· n`, (3) 그룹 완료율 툴팁 `완료 dc / 전체 n (pct%)`, (4) 상세 슬라이드오버 위치 인디케이터 `i/n`. 모듈 상단에 카운트 전용 포매터 `nfmt(n)=n.toLocaleString('ko-KR')`를 한 줄 추가하고 이 4개 지점에 적용해, 툴바 카운트·목록/상세 숫자(배치114·118·120)와 동일한 천단위 표기로 맞춤. 순수 표시 로직이라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 스코프 tsconfig(`tsconfig.check.json`) 타입검증 `error TS` 0건(rc=0), 호스트 전체 `tsc --noEmit -p tsconfig.json`(44s 캡)도 `error TS` 0건(rc=0) 완주. 작업 전 src 백업(/tmp/bak_1785261902). 마운트(=OneDrive 실파일) 무결성 재확인 — 깨진문자(U+FFFD) 0, `nfmt` 6줄(정의 1 + 적용 5) 삽입 확인.


## 2026-07-28 (배치 120 — 배포 대기, 검색·필터 무결과 빈 상태 안내 분리 + 항목 카운트 천단위 쉼표)
- ⑨ **목록 공통: (1) 검색·필터로 결과가 0건일 때의 빈 상태 문구를 도메인 안내(emptyText)와 분리, (2) 툴바 항목 카운트 천단위 쉼표 일관화** — (1) 배치117에서 요구사항·리스크·회의 등 5개 화면에 `emptyText`(도메인 등록 안내)를 붙였는데, ResourceView 빈 상태 렌더가 `emptyText || (검색/필터 문구)` 순서라 **검색·필터 결과가 0건이어도 emptyText가 우선**해, 사용자가 "xyz"로 검색해 매칭이 없을 때 "요구사항을 등록해 RTM 추적…" 같은 등록 안내가 떠 마치 목록이 비어 있는 듯 오인시켰음. 렌더 조건을 `(q||filter) ? '조건에 맞는 … 없습니다. 검색어나 필터를 조정해 보세요.' : (emptyText || 기본 등록 안내)`로 뒤집어, 진짜 빈 목록에만 도메인 emptyText를 보여주고 필터 무결과에는 조정 안내를 표시하도록 통일(배치117 emptyText 의도 보존). (2) 툴바 카운트 배지(`n건`·`표시/전체건`·hover 툴팁)의 숫자를 `toLocaleString('ko-KR')`로 감싸 배치114·118의 천단위 쉼표 표기와 일관화. 순수 표시 로직이라 데이터·API·스키마·Props 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 편집 대상 ResourceView.tsx를 포함하는 스코프 tsconfig(`tsconfig.check.json`)로 타입검증 — `error TS` 0건(rc=0, 8s 완주). 절차상 호스트 전체 `tsc --noEmit -p tsconfig.json`(44s 캡)도 실행 — `error TS` 0건. 작업 전 src 백업(/tmp/bak_1785240303). 마운트(=OneDrive 실파일) 무결성 재확인 — 깨진문자(U+FFFD) 0, `toLocaleString('ko-KR')` 8건·`검색어나 필터를 조정` 삽입 확인.


## 2026-07-28 (배치 119 — 배포 대기, 그룹화 헤더 상태·우선순위·유형 값 한글 라벨화)
- ⑨ **목록 공통: 그룹화 헤더의 상태·우선순위·유형 값을 한글 라벨로 표기(배지·상태필터와 일관화)** — 목록을 상태/우선순위/유형 등으로 그룹화하면 배지 셀·상태 필터 드롭다운(배치18)·일괄작업 바는 모두 LABEL(한글)로 표시되는데, **그룹 헤더 텍스트만 원시 영문 코드**(예: `open`·`in_progress`·`high`·`bug`)를 그대로 노출해 같은 값이 화면 안에서 영문↔한글로 어긋나 보였음. ResourceView 그룹 헤더 렌더에서 표시 텍스트를 `{g}`→`{LABEL[g] || g}`로 바꿔, 코드값이 LABEL에 있으면 한글(열림·진행중·높음·결함 등)로, 담당자·에픽·분류 등 자유입력 값은 그대로 폴백 표기하도록 보강. 그룹 좌측 색상 바(STATUS_COLOR[g])·접기/펼치기·완료율 집계는 **원시 코드값(g)을 그대로 키로 사용**하므로 무영향(표시 텍스트만 변경). LABEL은 이미 import돼 있어 추가 import·Props·API·스키마 변경 없음, 순수 표시 로직이라 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: `tsc --noEmit -p tsconfig.json`(호스트 전체) 통과 — `error TS` 0건(exit 0, 이번 실행은 44s 캡 내 완주). 스코프 tsconfig(`tsconfig.check.json`)도 error 0건 교차확인. 작업 전 src 백업(/tmp/bak_1785175579). 마운트(=OneDrive 실파일) 무결성 재확인 — 깨진문자(U+FFFD) 0, `LABEL[g] || g` 1건 삽입 확인.


## 2026-07-27 (배치 118 — 배포 대기, 목록 표 숫자·금액 컬럼 천단위 쉼표 일관화)
- ⑨ **목록 공통: 표(리스트) 숫자·금액 컬럼에 천단위 쉼표 표기(상세·폼과 일관화)** — 배치114에서 상세 슬라이드오버의 숫자·금액 `<dd>` 값을, 입력 폼은 `comma` 필드를 이미 `toLocaleString`으로 천단위 쉼표를 찍었지만, **목록 표 셀은 커스텀 `render`가 없는 숫자(type:'number')·`comma` 컬럼을 원시 숫자 그대로 노출**(예: 계약금액 `850000000`, 단가 `1200000`)해 같은 값이 목록↔상세↔폼에서 다르게 보이는 불일치가 남아 있었음. ResourceView의 목록 셀 기본 렌더(커스텀 render·배지·mono·마감일 분기에 해당 없는 마지막 분기)에서, 해당 컬럼의 필드가 `type:'number'`이거나 `comma:true`이고 값이 유한수일 때만 `Number(v).toLocaleString('ko-KR')`로 포매팅해 표기하도록 보강(배치114의 상세 규칙과 동일 술어). WBS 번호·강조(strong) 표기, 값 없을 때 `—` 폴백은 그대로. 커스텀 `render`가 있는 컬럼은 기존 로직 유지(무영향). 순수 표시 로직이라 데이터·API·스키마 무영향, Props 인터페이스·소비 페이지 무변경, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 편집 대상 ResourceView.tsx를 포함하는 스코프 tsconfig(`tsconfig.check.json`)로 타입검증 — `error TS` 0건(rc=0). 전체 `tsc`는 이 환경의 bash 실행 상한(44s)을 초과(배치113~117과 동일 제약)해 최종 게이트는 `배포.ps1`의 호스트 전체 `tsc --noEmit -p tsconfig.json`가 담당. 작업 전 src 백업(/tmp/bak_1785153880). 마운트(=OneDrive 실파일) 무결성 재확인 — 깨진문자(U+FFFD) 0, `fields.find`로 필드 조회·`toLocaleString` 삽입 확인.


## 2026-07-27 (배치 117 — 배포 대기, 목록 빈 상태 문구 도메인 맞춤 일관화)
- ⑨/⑥ **목록 공통: 요구사항·리스크·회의·인터페이스·산출물양식 5개 화면의 빈 상태(empty) 문구를 도메인 맞춤으로 통일** — ⑥의 "각 목록 빈 상태 문구 도메인 맞춤 통일"은 대부분 완료됐지만, 이 5개 목록은 `emptyText` prop이 없어 ResourceView 기본 문구(`아직 등록된 {title} 항목이 없습니다. "새로 만들기"로 추가하세요.`)로 떨어져, 이슈·테스트·기성고·To-Do·조달 등 도메인 안내(무엇을 왜 등록하는지)를 주는 화면들과 톤이 어긋났음. 각 ResourceView 호출에 `emptyText` 한 줄만 추가 — 요구사항(우선순위·인수기준 정의로 RTM 추적), 리스크(발생가능성·영향도·대응방안 기록), 회의(안건·결정사항·후속조치 기록), 인터페이스(송·수신 시스템·연동 규격 등록), 산출물양식(템플릿 항목 구성 정의). 기존 도메인 문구와 동일한 "새로 만들기" 안내 톤·곡선따옴표(“ ”)로 맞춤. `emptyText`는 Props에 이미 선언된 선택 문자열 prop(`emptyText?: string`)이라 타입·컴포넌트·API·스키마 무영향, 순수 표시 문자열 추가라 마이그레이션 불필요. — src/app/{requirements,risks,meetings,interfaces,form-definitions}/page.tsx
- 검증: 편집이 선언된 선택 문자열 prop에 문자열 리터럴만 전달하는 변경(타입 오류 발생 여지 없음)이고, 전체 `tsc`는 이 환경에서 실행 상한(45s bash 캡)을 초과(배치113~116과 동일 제약)해, 편집한 5개 페이지+ResourceView만 검사하는 스코프 tsconfig(`tsconfig.check.json`)로 타입검증 — `error TS` 0건(rc=0). 최종 게이트는 `배포.ps1`의 호스트 전체 `tsc --noEmit -p tsconfig.json`가 담당. 작업 전 src 백업(/tmp/bak_wk_1785111269). 마운트(=OneDrive 실파일) 무결성 재확인 — 5개 파일 깨진문자(U+FFFD) 0, `emptyText` 각 1건 삽입 확인. 스코프 검증 임시파일(tsconfig.check.json)은 이미 `.gitignore` 등재(커밋 제외).

## 2026-07-27 (배치 116 — 배포 대기, 모바일 터치 타깃 44px 보강)
- ⑨ **접근성·모바일: 신규 화면 목록 44px 터치 타깃 보강** — 기성고·산출물양식(form-definitions)·테스트차수·To-Do 등 신규 화면은 모두 공통 목록 컴포넌트(ResourceView)를 쓰는데, 모바일에서 목록의 주요 인터랙티브 요소가 손가락 탭 기준(44px)보다 작았음: 행 액션(수정·삭제) 버튼은 `btn-sm`(≈28px)이라 터치 기기에서 항상 노출되지만 오탭이 잦았고, 선택 열·컬럼 토글 체크박스는 기본 13px, 하단 빠른 추가 입력칸은 인라인 `height:30`, 인라인 편집 드롭다운(`select.sel`)은 `padding:2px`로 얕고, 상태·우선순위 배지 셀(인라인 편집 트리거)도 세로 탭 영역이 좁았음. globals.css에 **v23 모바일(≤860px) 전용 블록**을 추가해 `.btn`(44px)·`.btn-sm`(40px)·`.iconbtn`(44×44)·`.seg button`(40px)·행 액션 버튼(40px·좌우 패딩)·목록 체크박스(20×20)·빠른추가 입력(min-height 40)·인라인 select(min-height 38)·배지 셀(상하 패딩)의 탭 영역을 확보. `min-height`가 요소의 인라인 `height`보다 우선하므로 `!important` 없이 보정됨. 모바일 미디어쿼리 한정 **순수 CSS**라 데스크톱 레이아웃·데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/globals.css
- 검증: 변경이 `.ts/.tsx`를 전혀 건드리지 않는 **CSS 전용**이라 타입 영향 0. 절차상 `tsc --noEmit -p tsconfig.json` 실행 — `error TS` 0건, 다만 호스트/마운트 tsc가 44s 실행 상한을 초과(rc=124, 배치113~115와 동일한 환경 제약)해 완주 전 종료됨. 최종 게이트는 `배포.ps1`의 호스트 전체 tsc가 담당. 작업 전 src 백업(/tmp/bak_1785110147). 마운트(=OneDrive 실파일) 무결성 재확인 — globals.css 436줄, 깨진문자(U+FFFD) 0.

## 2026-07-25 (배치 115 — 배포 대기, 상세 슬라이드오버 연계 값 클릭 이동 링크)
- ⑨ **상세 슬라이드오버: 연계 요구사항·테스트 차수·관계 이슈·선행 업무 값을 클릭 이동 링크로** — 상세 패널의 `<dd>`는 지금까지 연계 코드(예: 테스트의 `reqCode`·`cycle`, 이슈의 `reqCode`·`related`, 업무의 `reqCode`·`predecessor`)를 **평범한 텍스트로만** 보여줘서, "이 테스트가 검증하는 요구사항이 무엇인지", "이 이슈와 엮인 이슈가 어디 있는지" 확인하려면 사용자가 값을 눈으로 외워 다른 화면으로 이동·검색해야 했음. ResourceView 상세 렌더에 entity별 연계 필드→대상 목록 매핑(`{ tests:{reqCode:'/requirements', cycle:'/test-cycles'}, issues:{reqCode:'/requirements', related:'/issues'}, tasks:{reqCode:'/requirements', predecessor:'/tasks'} }`)을 두고, 해당 필드에 값이 있으면 값을 `<a href="/대상?q=<값>">`(브랜드색·밑줄·툴팁) 링크로 표기. 클릭하면 대상 목록으로 이동하고, 배치113에서 도입한 **URL 쿼리(q) 복원**이 그 값으로 검색을 걸어 연계 항목을 바로 보여줌(대상 목록 검색은 모든 컬럼값을 매칭하므로 코드/차수명이 그대로 걸림). Pill(상태·우선순위) 필드와 마감 D-day 강조 경로는 그대로 두고, 값이 없으면 기존대로 `—`. 기존 삼항 렌더를 명시적 분기(`cell`)로만 정리한 순수 표시 변경이라 데이터·API·스키마 무영향, 마이그레이션 불필요. 컴포넌트 Props 인터페이스 무변경(소비 페이지 영향 없음). 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc`가 45s 실행 상한(마운트/호스트 tsc 환경)을 초과해, ResourceView 서브그래프만 검사하는 스코프 tsconfig(`tsconfig.check.json`)로 타입검증(error TS 0건). 추가 import 없음(entity·JSX·encodeURIComponent만 사용), Props 무변경이라 소비 페이지 영향 없음이 근거이며 최종 게이트는 `배포.ps1`의 호스트 전체 `tsc --noEmit -p tsconfig.json`가 담당. 작업 전 src 백업(/tmp/bak_wk_1784959374). 마운트(=OneDrive 실파일) 무결성 재확인 — 508줄, 깨진문자(U+FFFD) 0.

## 2026-07-25 (배치 114 — 배포 대기, 상세 슬라이드오버 숫자·금액 천단위 쉼표 일관화)
- ⑨ **상세 슬라이드오버: 숫자·금액 필드 천단위 쉼표 표기(목록·폼과 일관화)** — 목록 컬럼(예: 조달 수량·계약금액)과 입력 폼(comma 필드)은 이미 `toLocaleString`으로 천단위 쉼표를 찍었지만, **상세 슬라이드오버의 `<dd>` 값만 원시 숫자를 그대로 노출**해(예: 계약금액 `850000000`, 단가 `1200000`) 같은 값이 화면마다 다르게 보이는 불일치가 있었음. ResourceView 상세 패널 렌더에서 필드가 `type:'number'`이거나 `comma:true`이고 값이 유한수일 때만 `Number(v).toLocaleString('ko-KR')`로 포매팅(`dispV`)해 표기하도록 보강. 상태·우선순위 Pill, 날짜, 마감 D-day/초과 강조 경로는 그대로 두고, 값이 없으면 기존대로 `—`. 진척률·공수·수량·계약금액·단가 등 모든 숫자 필드가 목록·폼과 동일한 천단위 표기로 통일됨. 순수 표시 로직이고 Props 인터페이스·소비 페이지 무변경, 데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/components/ResourceView.tsx
- 검증: 전체 `tsc`가 45s 실행 상한(마운트/호스트 tsc 환경)을 초과해, ResourceView 서브그래프만 검사하는 스코프 tsconfig(`tsconfig.check.json`, incremental off·types 비움)로 타입검증(error TS 0건). Props 무변경이라 소비 페이지 영향 없음이 근거이며 최종 게이트는 `배포.ps1`의 호스트 전체 `tsc --noEmit -p tsconfig.json`가 담당. 작업 전 src 백업(/tmp/bak_wk_1784956634). 마운트(=OneDrive 실파일) 무결성 재확인 — 499줄, 깨진문자(U+FFFD) 0. 스코프 검증 임시파일(tsconfig.check.json)은 이미 `.gitignore`에 등재(커밋 제외).

## 2026-07-25 (배치 113 — 배포 대기, 목록 뷰 상태를 URL 쿼리로 공유)
- ⑨ **목록 공통: 활성 검색·상태필터·정렬·그룹을 URL 쿼리에 반영(공유 가능한 뷰 링크)** — 그동안 검색·필터·정렬·그룹은 컴포넌트 state(+일부 localStorage)에만 있어 **지금 보고 있는 뷰를 링크로 남에게 공유하거나 새로고침으로 보존할 수 없었음**. ResourceView에 (1) 마운트 시 `window.location.search`를 읽어 `q`(검색)·`status`(상태필터)·`sort`+`dir`(정렬)·`group`(그룹)이 있으면 localStorage보다 **우선**해 상태를 복원, (2) 이 값들이 바뀔 때마다 내비게이션 없이 `history.replaceState`로 URL 쿼리를 갱신(다른 페이지 파라미터는 보존, 기본값이면 키 삭제), (3) 툴바 초기화 옆에 활성 뷰가 있을 때만 **'뷰 링크' 복사 버튼**(클립보드 복사 후 '복사됨' 피드백)을 추가. 이제 URL만 공유하면 상대가 동일한 검색·필터·정렬·그룹 상태의 목록을 그대로 보게 됨. 순수 클라이언트 표시·상태 동기화라 데이터·API·스키마 무영향, 마이그레이션 불필요. 컴포넌트 Props 인터페이스 무변경(소비 페이지 영향 없음). 단일 파일. — src/components/ResourceView.tsx
- 검증: 마운트가 파일 삭제(unlink)를 막고 전체 `tsc`가 45s 실행 상한을 초과하는 환경 제약으로, ResourceView 하위 그래프만 검사하는 스코프 tsconfig로 타입검증(27s, error TS 0건) — Props 무변경이라 소비 페이지 영향 없음이 근거. 최종 게이트는 `배포.ps1`의 호스트 전체 `tsc --noEmit -p tsconfig.json`가 담당. 작업 전 src 백업(/tmp/bak_1784955212). 마운트(=OneDrive 실파일) 무결성 재확인 — 496줄, 깨진문자(U+FFFD) 0. 스코프 검증 임시파일(tsconfig.check.json·zzz_del.json)은 마운트 삭제 불가로 남아 `.gitignore`에 추가(커밋 제외).

## 2026-07-24 (상용화 P0 — 배포 대기, /api/health 헬스체크 + 마켓플레이스 제출자료 초안)
- **공통 P0(모니터링): `GET /api/health` 공개 헬스체크 엔드포인트 신설** — 업타임 모니터·로드밸런서·마켓플레이스 상태점검이 붙을 표준 헬스체크가 없었음. Neon DB를 `select 1`로 가볍게 핑하고 `{ ok, status, service, version, time, uptimeSec, checks.db{ ok, latencyMs } }`를 반환. DB 정상 200 / 이상 시에도 크래시 없이 표준 JSON으로 **503(degraded)** 반환, `Cache-Control: no-store`. 인증 불필요(공개). 프로세스 업타임·`APP_VERSION`(기본 0.12.0) 노출. 읽기 전용·신규 테이블/스키마 무영향. — src/app/api/health/route.ts (신규)
- **상용화(마켓플레이스): 제출자료 초안 문서화** — 서비스 개요·주요기능·아키텍처(Next14+Drizzle+Neon+Vercel)·보안/컴플라이언스(RBAC·감사로그·멀티테넌시·표준에러·플래그 OFF)·요금제 3단계·SLA 초안·제출 체크리스트. 민감/법무 확정 항목은 **[승인 필요]** 표기. — docs/MARKETPLACE_SUBMISSION.md (신규)
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). host Read로 편집 2파일 완전성 확인(health 42줄·doc 말미 온전). 실결제·실인증 활성화 없음(플래그 OFF 유지).


## 2026-07-23 (배치 112 — 배포 대기, 리포트 프로젝트별 예산·기성·조달 집계 섹션 + 엑셀 시트)
- ⑨ **리포트: 프로젝트별 예산/기성 집계 섹션 + 엑셀 시트 1개 추가** — 리포트에는 진척·품질·팀 지표는 풍부했지만 **‘돈’ 관점의 프로젝트 횡단 집계(계약금액·기성·조달)가 전무**해, 재무 현황은 프로젝트를 하나씩 돌거나 조달/기성고 화면을 각각 봐야 했음. `/api/procurement`를 리포트 로드에 추가(기존 8종과 함께 병렬 페치)하고, 프로젝트별로 계약금액(projects.budget)·**최신 스냅샷 기성률**(snapshots를 snapshotDate 내림차순 정렬해 선택)·기성금액(계약금액×기성률)·조달총액(procurement qty×unitPrice 합)·입고액·예산대비 조달비율을 순수 클라이언트 집계. ‘프로젝트별 예산·기성·조달 집계’ 카드(기성률·예산대비 조달 바, 예산 초과 시 빨강, 스냅샷 없으면 안내, 합계행)를 스냅샷 추이 카드 아래에 배치하고, 재무 데이터(예산·조달·스냅샷)가 하나도 없는 프로젝트는 제외·전무하면 섹션 자체를 숨김. 엑셀 내보내기에도 **‘예산기성’ 시트**(프로젝트·계약금액·기성률·기성금액·조달총액·조달입고액·예산대비조달%) 1개를 추가. 읽기 전용 계산·표시라 데이터·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/reports/page.tsx
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784829942). 마운트(=OneDrive 실파일) 무결성 재확인 — 307줄, 깨진문자(U+FFFD) 0. won() 통화 포매터·finance/finTot 집계·procurement 페치 추가.


## 2026-07-23 (배치 111 — 배포 대기, 인쇄/PDF 레이아웃 정돈)
- ⑨ **인쇄/PDF: 리포트·프로젝트 요약 인쇄 레이아웃 정돈(no-print 정리)** — 리포트에는 `window.print()` 버튼이 있었지만 **프로젝트 상세(/projects/[id])에는 인쇄 진입점이 없었고**, 공통 print CSS도 애니메이션이 걸린 진척 바(pbar)가 인쇄 시 0%로 찍히거나 표가 스크롤 영역에 잘리고 카드가 페이지 경계에서 쪼개지는 등 정돈이 필요했음. (1) 프로젝트 상세 헤더에 **‘인쇄 / PDF’ 버튼**(no-print)을 추가하고 뒤로가기와 함께 no-print 행으로 묶어 인쇄물에서 버튼이 빠지게 함. (2) globals.css의 `@media print`를 정돈 — `@page{margin:14mm}` 여백, `.mobiletabs`(모바일 탭바) 숨김 추가, `*{transition:none!important;animation:none!important;print-color-adjust:exact}`로 진척 바·컬러 배경이 계산된 최종 상태·색으로 인쇄되게 하고, `.card/.kpi/.g2/.kpis/.dash-card/table/tr/thead`에 `break-inside:avoid`·`.sect`에 `break-after:avoid`로 카드·표가 페이지 경계에서 쪼개지지 않게, `.tbl-wrap{overflow:visible}`로 표 잘림 방지. 순수 표시·스타일이라 데이터·API·스키마 무영향, 마이그레이션 불필요. — src/app/projects/[id]/page.tsx, src/app/globals.css
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784808314). 마운트(=OneDrive 실파일) 무결성 재확인 — 깨진문자(U+FFFD) 0. lucide Printer 아이콘 import 추가.


## 2026-07-22 (배치 110 — 배포 대기, 프로젝트 상세 재무 요약 카드)
- ⑨ **프로젝트 상세(/projects/[id]): 계약금액·기성률·조달총액 재무 요약 카드 추가** — 상세에는 단계·업무·이슈·리스크·SPI·EVM·테스트 카드는 있었지만 **‘돈’ 관점(계약금액·기성 진행·조달 집행)을 한눈에 보는 요약이 없어**, 재무 상태 파악은 조달/기성고 화면을 각각 돌아야 했음. `/api/project-summary`에 **finance** 블록을 추가해 계약금액(projects.budget), 조달총액(procurement_items 수량×단가 합)·입고분·건수·예산대비 비율, 최신 스냅샷(snapshots) 기준 기성률·기성금액을 순수 집계로 계산(신규 테이블·쓰기 없음). 상세 화면에는 계약금액·기성률(스냅샷 기준일 표기·기성금액 부기·기성 진행 바)·조달총액(예산 대비 %·입고액, 초과 시 빨강) 3열 재무 요약 카드를 EVM 카드 위에 배치. 재무 데이터가 하나도 없으면 카드 자체를 숨김(빈 상태 안전). 읽기 전용 계산·표시라 데이터·스키마 무영향, 마이그레이션 불필요. — src/app/api/project-summary/route.ts, src/app/projects/[id]/page.tsx
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784743512). 마운트(=OneDrive 실파일) 무결성 재확인 — route 77줄·page 194줄, 깨진문자(U+FFFD) 0. procurementItems·snapshots 스키마 import 추가, won() 통화 포매터 추가.

## 2026-07-22 (배치 109 — 배포 대기, 알림 종류별 그룹(접기/펼치기))
- ⑨ **알림(/notifications): 종류별 그룹(접기/펼치기) 추가** — 알림 화면은 이미 유형 배지·유형 필터(건수 표기)·‘모두 읽음’ 일괄 처리·검색·안읽음만 토글을 갖췄으나, 종류가 섞인 **평면 목록**이라 "지금 마감 초과가 몇 건이고 어디에 몰렸는지"를 한눈에 묶어 보기 어려웠음. 툴바에 **‘종류별 그룹’ 토글**(기본 꺼짐)을 추가해, 켜면 마감 초과→마감 임박→결재 대기→일반 순으로 **색상 배지·건수·안읽음 수를 단 그룹 헤더** 아래로 알림을 묶고, 헤더(▾/▸)를 눌러 그룹별로 접거나 펼칠 수 있게 함. 기존 검색·유형/안읽음 필터가 적용된 `view`를 그대로 그룹핑하므로 필터 결과와 어긋나지 않고, 행 렌더링은 `renderRow`로 추출해 평면·그룹 모드가 동일 UI를 공유. 순수 클라이언트 표시(신규 API·페치·상태 변경 없음)라 데이터·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/notifications/page.tsx
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784721894). 마운트(=OneDrive 실파일) 무결성 재확인 — 214줄, 깨진문자(U+FFFD) 0. Fragment(key 부여) import 추가.

## 2026-07-22 (배치 108 — 배포 대기, 대시보드 담당자별 업무 부하·진척 미니 위젯)
- ⑨ **대시보드: 담당자별 업무 부하·진척 미니 위젯 추가** — 대시보드에 조직/이슈/리스크/업무 분포·내작업·마감임박·To-Do·최근 프로젝트 카드는 있었지만 **‘누가 얼마나 업무를 쥐고 있고 얼마나 진행됐는지’를 한눈에 보는 담당자 스코프 요약이 대시보드에 없어**, 부하 편중 파악은 별도 /workload 화면으로 이동해야만 가능했음. 이미 `/api/dashboard`가 내려주는 **tasks를 재사용**(신규 API·페치 없음)해 담당자(assignee)별로 진행중(status≠done)·완료·전체·평균 진척을 순수 클라이언트 집계하고, 진행중 건수 내림차순 상위 6명을 미니 바로 표시(담당자명·진행 N·완료 done/total·평균 진척 %·부하 비례 컬러 바). 최다 대비 70%↑는 빨강, 40%↑ 주황, 그 외 초록으로 부하 편중을 즉시 색출하고 카드/행 클릭 시 /workload로 이동. 담당자 미지정 업무는 ‘미지정’으로 묶고, 배정 업무가 없으면 빈 상태 안내. 읽기 전용 계산·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/dashboard/page.tsx
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784657122). 마운트(=OneDrive 실파일) 무결성 재확인 — 238줄, 깨진문자(U+FFFD) 0.

## 2026-07-21 (배치 107 — 배포 대기, 캘린더 담당자 필터·‘내 일정만’ 토글)
- ⑨ **통합 캘린더(/calendar): 담당자 필터 + ‘내 일정만’ 토글 추가** — 그동안 캘린더는 종류(회의·업무마감·테스트/이슈 기한) 필터만 있고 **담당자별로 걸러 볼 수단이 없어**, 팀 프로젝트에서 “내가 마감·기한을 쥔 일정만” 보기가 불가능했음. 각 이벤트에 담당자(assignee)를 실어(업무·테스트·이슈; 회의는 담당자 개념 없음), 필터 바 우측에 ① 이벤트에 실제 등장한 담당자만 채운 **담당자 select**(전체/개인) ② 로그인 사용자 이름 기준 **‘내 일정만’ 토글**(사용자가 담당자 목록에 있을 때만 노출)을 추가. 종류 필터·기한 초과 배지·kindCount·overdue 집계 모두 담당자 스코프(scoped)를 반영해 표기가 어긋나지 않게 함. 로그인 사용자 이름은 기존 `/api/my-work`의 `name`을 재사용(신규 API 없음). 순수 클라이언트 필터라 데이터·스키마·기존 API 무영향, 마이그레이션 불필요. 단일 파일. — src/app/calendar/page.tsx
- 검증: `tsc --noEmit -p tsconfig.json` 통과(error TS 0건). 작업 전 src 백업(/tmp/bak_1784635527). 마운트(=OneDrive 실파일) 무결성 재확인 — 135줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0.

## 2026-07-21 (배치 106 — 배포 대기, 팀원 초대 링크)
- **팀원 초대 링크(코드 자동입력 URL)** — 초대 코드를 일일이 불러주지 않아도 되게, 설정 '초대 코드' 카드에 **'초대 링크 복사'** 버튼 추가(`{origin}/login?invite=CODE`). 팀원이 그 링크로 접속하면 로그인 화면이 **자동으로 회원가입 모드 + 초대 코드 자동입력**되어 이름·이메일·비번만 넣으면 같은 조직에 합류. — src/app/login/page.tsx(?invite= 파라미터 처리), src/app/settings/page.tsx(링크 복사 버튼)
- 검증: `tsc --noEmit --incremental` 통과(에러 0). 무결성 확인.

## 2026-07-21 (배치 105 — 배포 대기, ★ 로그인 불가 긴급 수정)
- **로그인 불가(가입/로그인 후 대시보드 진입 안 됨) 수정** — 직전 배치에서 세션 쿠키에 넣은 `secure: process.env.NODE_ENV==='production'` 플래그를 **되돌림**(기존 자동로그인 쿠키는 secure 없이 정상 동작했는데, secure 추가 후 신규 세션이 유지되지 않아 로그인→대시보드가 로그인으로 되튕기던 증상). 원래 동작하던 `{ httpOnly, sameSite:'lax', path, expires }`로 복원. — src/lib/auth.ts
- **복구용 관리자 부팅 보장(안전장치)** — 로그인 불가 상황 대비, 서버 기동 시 `admin@demo.local`/`admin1234` 계정이 없으면 자동 생성(멱등, 이미 있으면 비번 등 절대 미변경). 배포 후 이 계정으로 무조건 로그인 가능 → 이후 설정/관리에서 정리. — src/lib/bootstrap.ts(신규), src/instrumentation.ts
- 검증: `tsc --noEmit --incremental` 통과(에러 0). 무결성 확인.

## 2026-07-21 (배치 104 — 배포 대기, 폼 안내문 '—'→'·' 일괄 정리)
- ④ **폼 안내문 em대시(—) 정리** — 안내문 8개 파일 11곳의 어색한 ` — `(공백+em대시)를 ` · `(가운뎃점)로 일괄 교체(hint 문자열 내부만 안전하게, 툴팁·본문 미접촉). 방화벽·인프라·인력·조달·요구사항·기성고·테스트차수·테스트 폼의 안내가 배치101 줄간격 개선과 함께 깔끔해짐. — src/app/{firewall,infra,members,procurement,requirements,snapshots,test-cycles,tests}/page.tsx
- 검증: `tsc --noEmit --incremental` 통과(에러 0). 남은 hint em대시 0건 확인.

## 2026-07-21 (배치 103 — 배포 대기, ★ 조직 멤버 관리(비번 초기화·활성 토글))
- **관리자 멤버 관리 강화** — 실팀 운영에 필수인 두 기능을 사용자·권한(/admin) 화면에 추가. (1) **비밀번호 초기화** — 멤버가 비번을 잊었을 때(이메일 연동 없음) 관리자가 임시 비밀번호(`pms-xxxxxx`)로 초기화 → 화면에 표시되어 본인에게 전달, 이후 설정에서 변경 안내. (2) **활성/비활성 토글** — 퇴사·휴직자 계정을 비활성화(로그인·집계 제외), 본인 계정 비활성화는 차단. API(`/api/admin/users` PATCH)에 resetPassword·isActive 분기 추가(기존 역할 변경 유지, 대상 멤버십의 org 소속 검증), 화면에 '관리' 컬럼(비번 초기화·활성 토글 버튼 + 임시 비번 표시). — src/app/api/admin/users/route.ts, src/app/admin/page.tsx
- 검증: `tsc --noEmit --incremental` 통과(에러 0). 작업 전 src 백업, 무결성(깨진문자 0) 확인.

## 2026-07-21 (배치 102 — 배포 대기, ★ 팀원 초대(조직 합류))
- **팀원 초대 기능 신설** — 그동안 회원가입은 매번 새 조직을 만들어 같은 조직에 팀원을 넣을 수 없었음(실팀 사용 불가). 조직 **초대 코드** 방식으로 해결. (1) `organizations.inviteCode` 컬럼 신설(migrate ALTER 자동반영). (2) 회원가입 API: `inviteCode`가 오면 해당 코드의 조직을 찾아 **멤버(role:member)로 합류**(없으면 '초대 코드가 올바르지 않습니다'), 없으면 기존대로 새 조직 생성(관리자) + 초대코드 자동 발급. (3) 설정 API: 조직에 코드 없으면 관리자 GET 시 자동 생성, PATCH `regenerateInvite`로 재발급. (4) 설정 화면 '조직' 카드에 **초대 코드 표시·복사·재발급** UI + 안내. (5) 로그인 회원가입 폼에 **초대 코드 필드**(선택) 추가 — 코드 입력 시 조직명 생략 가능하도록 검증 완화. 이제 관리자가 코드를 공유하면 팀원이 같은 조직에 합류해 실팀 협업 가능. — src/db/schema.ts, src/lib/migrate.ts, src/app/api/auth/register/route.ts, src/app/api/settings/route.ts, src/app/settings/page.tsx, src/app/login/page.tsx
- 검증: `tsc --noEmit --incremental` 통과(에러 0). 작업 전 src 백업, 마운트 무결성(깨진문자 0) 확인.

## 2026-07-21 (배치 101 — 배포 대기, 폼 안내문(hint) 가독성 폴리시)
- ④ **폼 안내문(hint) 2줄 가독성 개선** — 폼 필드 아래 안내문이 2줄로 넘어갈 때 줄이 붙어 답답하던 것을, ResourceView 힌트 span에 `line-height:1.55`·여백·자간을 넣어 전 폼 공통으로 깔끔하게. 더불어 프로젝트 폼의 장황한 안내문을 간결화하고 어색한 em대시(—)를 가운뎃점(·)으로 교체: 계약금액 '원 단위 입력 · 기성·정산 집계의 기준 금액', 시작일 '시작·종료일 입력 시 계획 진척(경과율)이 계산됩니다'. — src/components/ResourceView.tsx, src/app/projects/page.tsx
- 검증: 파일 도구(Edit) 소규모 문자열/스타일 변경. (샌드박스 디스크 풀로 이번엔 tsc 미실행 → 배포.ps1 tsc 게이트가 최종 검증.)

## 2026-07-21 (배치 100 — 배포 대기, ★ 비밀번호 변경(실사용 필수))
- **비밀번호 변경 기능 신설** — 상용 오픈 필수인데 그동안 UI/API가 없었음(설정엔 "비밀번호를 변경하세요" 안내만 있고 실제 변경 불가). (1) `/api/profile` PATCH를 확장해 `{ currentPassword, newPassword }`가 오면 현재 비밀번호를 verifyPassword로 검증 → 새 비밀번호 8자↑ 확인 → hashPassword로 교체(기존 이름 변경 동작은 그대로 유지). (2) 설정 화면에 **비밀번호 변경 카드**(현재/새/새 확인 3필드 + 클라이언트 검증: 8자↑·일치 확인 + 결과 메시지) 추가. 이제 데모 계정 비번 변경·실사용자 비번 관리가 화면에서 가능. — src/app/api/profile/route.ts, src/app/settings/page.tsx
- 검증: 두 파일 TypeScript 구문검증 통과. 작업 전 src 백업, 무결성(깨진문자 0) 확인. verifyPassword/hashPassword export 확인. 배포는 배포.ps1 tsc 게이트 재검증.
- (참고: 조달 페이지 금액·발주/입고 검증 등 세부 폴리시는 야간 자동개발이 이미 고도화 완료.)

## 2026-07-21 (배치 99 — 배포 대기, 폼 통화 쉼표 포맷/정렬 폴리시)
- ④ **폼 통화 입력 실시간 천단위 쉼표 + 우측정렬** — 상용 품질 디테일. ResourceView 폼 필드에 `comma` 플래그 지원 추가(HTML `type=number`는 쉼표 표시가 불가하므로 텍스트 입력+`inputMode=numeric`으로 렌더, 입력 시 숫자만 남겨 저장·표시 시 `toLocaleString()`로 `850,000,000` 형태, 우측정렬). 프로젝트 **계약금액**·조달 **단가**에 적용. (목록 컬럼은 이미 쉼표 표기.) — src/components/ResourceView.tsx(comma 브랜치+타입), src/app/projects/page.tsx, src/app/procurement/page.tsx
- 검증: 4개 파일 TypeScript 구문검증 통과(JSX 정상). 전체 tsc는 샌드박스 시간초과로 미완주 → 배포.ps1 tsc 게이트가 최종 재검증(타입오류 시 자동 중단). 작업 전 src 백업, 무결성(깨진문자 0) 확인.

## 2026-07-21 (배치 98 — 배포 대기, ★ 인증 상용화: 공개 자동로그인 OFF)
- **실서비스 인증 정리** — 공개 자동로그인(누구나 접속 시 데모 admin으로 자동 로그인)을 비활성화. (1) `/api/auth/auto`를 무력화(항상 `{ ok:false, disabled:true }` 반환, 세션 생성 안 함) (2) 로그인 페이지 useEffect에서 자동로그인 호출 제거(이미 로그인된 세션이면 대시보드로만 이동) (3) 로그인 하단 데모 자격증명 노출 문구를 "회원가입으로 관리자 계정을 만드세요" 안내로 교체. 이제 **정상 로그인/회원가입만 허용** — 첫 회원가입 계정이 조직 관리자(admin). 데모 계정(admin@demo.local)은 남아있으므로 가입 후 설정에서 비밀번호 변경/삭제 권장. — src/app/login/page.tsx, src/app/api/auth/auto/route.ts
- 검증: 변경이 코드 제거/최소 핸들러라 저위험. 전체 tsc는 샌드박스 시간제한으로 미완주 → 두 파일 무결성(깨진문자 0)·구문 정상 확인. 배포는 배포.ps1의 tsc 게이트가 재검증(타입오류 시 자동 중단).

## 2026-07-21 (배치 97 — 배포 대기, 감사 로그(/audit) 영역 필터 건수 표기 + '오늘 변경' 요약 칩)
- ⑥/④ **감사 로그(/audit): 영역 필터가 동작 필터와 달리 건수를 안 보여주고, 감사 모니터링 화면인데 '오늘 얼마나 바뀌었는지' 지표가 전무하던 누락 보강** — /audit 툴바의 **동작(action) 필터**는 이미 `생성 (N)·수정 (N)·삭제 (N)`로 옵션마다 건수를 노출하는데, 정작 바로 옆 **영역(entity) 필터**는 영역명만 나열해 어느 영역에서 변경이 몰렸는지(=필터 선택 전 사전 파악)를 알 수 없었고, 감사 로그는 '최근 100건'만 보여줄 뿐 **오늘 하루 발생한 변경 총량**이 어디에도 없어 일일 활동 모니터링이 어려웠음. ① `entCounts` 메모(영역별 건수 집계)를 추가해 영역 필터 옵션을 `{영역명} ({N})` 형태로 바꿔 동작 필터와 표기 규칙을 일치시킴(로그에 실제 등장한 영역만 노출하던 기존 로직 유지). ② `todayCount` 메모(로컬 날짜 기준 오늘 createdAt 집계, `Number.isFinite` 가드로 잘못된 날짜 배제)를 추가해, 로드 완료·오늘 변경 1건 이상일 때만 툴바 우측 요약 앞에 `오늘 N건`(테라코타 강조) 칩과 근거 툴팁을 노출. 읽기 전용 클라이언트 계산·표시라 데이터·API(/api/audit)·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/audit/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~96이 전 화면(목록·리포트·대시보드·관리·설정·프로젝트 상세·로그인·랜딩)을 일순한 뒤, 표기 일관성·모니터링 지표가 미비하던 /audit을 재방문(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784574277). Edit 도구로 호스트 원본에 직접 기록 후 마운트(=OneDrive 실파일) 무결성 재확인 — 151줄(+19), 꼬리 잘림 없음·깨진문자(U+FFFD) 0.

## 2026-07-20 (배치 96 — 배포 대기, 랜딩(/·/lp — LpClient) 스티키 네비 앵커 오프셋 결함 수정 — 상단 메뉴 클릭 시 섹션 제목이 고정 네비 뒤에 파묻히던 문제)
- ④/② **랜딩(LpClient): CHANGELOG에 한 번도 등장하지 않은 마지막 미접촉 화면의 인앵커 스크롤 오프셋 결함 수정** — /와 /lp가 함께 렌더하는 마케팅 랜딩(LpClient.tsx, 629줄)은 배치 이력상 처음 손대는 화면으로, 상단 네비(`.lp-nav`)가 `position:sticky; top:0`(높이 66px)로 고정돼 있는데 정작 그 네비의 메뉴(도입 이유·핵심 기능·진행 방식·FAQ)가 가리키는 인페이지 앵커(`#why`·`#features`·`#flow`·`#faq`)에는 **스크롤 오프셋이 전혀 없어**, 메뉴를 클릭하면 대상 `<section id>`가 66px 고정 네비 **뒤로 파묻혀 섹션 제목이 잘려 보이던** 흔한 앵커 결함이 있었음(모바일에서도 상단 네비는 그대로라 동일 증상). 앵커 대상 섹션에만 스코프된 `.lp section[id]{scroll-margin-top:78px}`(네비 66px + 여백 12px) 한 줄을 추가해, 메뉴 클릭·해시 이동 시 섹션 제목이 네비 아래에서 온전히 드러나게 함. 순수 CSS(문자열 상수 `css` 내부)라 JS 로직·컴포넌트 구조·데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/lp/LpClient.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~95가 앱 내부 전 화면(목록·리포트·대시보드·관리·설정·프로젝트 상세·로그인)을 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 유일한 남은 화면인 마케팅 랜딩(LpClient)**을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784552677). Edit 도구로 호스트 원본에 직접 기록 후 마운트(=OneDrive 실파일) 무결성 재확인 — 631줄(+2), 꼬리 잘림 없음·깨진문자(U+FFFD) 0.

## 2026-07-20 (배치 95 — 배포 대기, 로그인/회원가입(/login) 클라이언트 입력 검증 — 빈 값·형식 오류 서버 왕복 전 색출 + 제출 버튼 게이트)
- 4/6 **로그인(/login): CHANGELOG에 한 번도 등장하지 않은 인증 화면의 무검증 제출 함정 수정** — /login은 배치 이력상 처음 손대는 화면으로, 로그인/회원가입 폼이 **아무 클라이언트 검증 없이** 제출돼(버튼 disabled={busy}만), 이메일을 비웠거나 형식이 틀렸거나 비밀번호가 8자 미만이어도(placeholder '8자 이상'만 안내) 일단 서버로 보내고 나서야 오류를 받는 구조였음 — 배치94가 /settings 조직명·프로필에 넣은 '빈 값·무변경 시 버튼 비활성' 규칙이 정작 가장 먼저 만나는 인증 화면에는 빠져 있던 누락. (1) 이메일 형식·비밀번호 8자 이상·(회원가입 시)이름·조직명 필수를 클라이언트에서 계산(emailOk·pwOk·regOk→canSubmit)해 **제출 버튼을 미충족 시 비활성** + title 툴팁으로 사유 안내. (2) 폼 하단에 첫 번째 미충족 사유를 테라코타(#be5535) **인라인 힌트**로 표시(이메일·비밀번호 둘 다 빈 초기 화면에서는 안내 억제해 과한 잔소리 방지). (3) submit() 진입부에 if(!canSubmit) 가드를 넣어 Enter 제출·자동완성 우회 시에도 서버 왕복 없이 즉시 사유를 노출. 데모 계정(admin@demo.local / admin1234)은 형식·길이 모두 충족해 영향 없음. 순수 클라이언트 표시·게이트라 인증 API·auto-login useEffect·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/login/page.tsx
- 6 안전 백로그·8 monday UX·5 테스트 모두 [x] 소진, ROADMAP 잔여 [ ]는 대형/주간 수동(7·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~94가 앱 내부 화면(목록·리포트·대시보드·관리·설정·프로젝트 상세)을 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 미접촉 화면 /login**을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 [ ] 항목 없음).
- 검증: tsc --noEmit 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784487945). Edit 도구로 호스트 원본에 직접 기록 후 마운트(=OneDrive 실파일) 무결성 재확인 — 64줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0.

## 2026-07-19 (배치 94 — 배포 대기, 설정(/settings) 역할·플랜 한글 라벨 통일 + 조직명 빈 값/무변경 저장 함정 방지 + 프로필 저장 가드)
- ⑥/④ **설정(/settings): CHANGELOG에 한 번도 등장하지 않은 마지막 미접촉 화면의 역할·플랜 영문 노출·무음 저장 함정 수정** — /settings는 배치 이력상 처음 손대는 화면으로, ① '내 역할'과 '플랜' 배지가 저장 원문(admin/pmo/pm/member, free/pro…)을 **영문 그대로** 출력해 배치18·79·81·92의 한글 라벨화 규칙에서 누락돼 있었고, ② 조직명 input을 비운 채 '저장'을 눌러도 API(/api/settings PATCH)가 `if(name)`으로 **빈 값을 조용히 무시**하는데 버튼은 '저장됨 ✓'을 띄워 실제로는 저장 안 된 걸 알 수 없는 무음 저장 함정이 있었으며, ③ 프로필 '표시 이름'은 빈 값이면 API가 400을 던지는데도 버튼이 그대로 활성이었음. ① 역할·플랜 라벨/색상을 **settings 화면 로컬로만** 정의(`ROLE_LABEL`·`ROLE_BADGE`·`PLAN_LABEL`, 배치92 /admin과 동일 패턴)해 한글(관리자·PMO·PM·멤버 / 무료·프로·팀·비즈니스·엔터프라이즈)로 통일 — **전역 ui.tsx 무접촉**(pm='PM승인' 쓰는 테스트 화면 영향 없음), 원문은 title 툴팁에 보존. ② 조직명 저장 버튼을 **빈 값·무변경 시 비활성**으로 바꾸고 그 이유(비워둘 수 없음 / 변경된 내용 없음)를 안내 문구로 명시. ③ 프로필 저장 버튼도 이름이 비면 비활성. 순수 클라이언트 표시·검증이라 데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/settings/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~93이 목록·리포트·대시보드·관리·프로젝트 상세를 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 마지막 미접촉 화면 /settings**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784466334). 마운트(=OneDrive 실파일)에 python(encoding='utf-8')으로 직접 기록 — 49줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0, 호스트 원본(Read) 재확인.

## 2026-07-19 (배치 93 — 배포 대기, 프로젝트 상세(/projects/[id]) 헤더 날짜 구간에 D-day·초과 칩 추가)
- ⑥/③ **프로젝트 상세(/projects/[id]): 한 번도 손대지 않은 상세 대시보드 헤더의 날짜 구간이 원시 문자열뿐이라 마감 임박·초과가 드러나지 않던 누락 수정** — /projects/[id]는 배치 이력상 처음 손대는 화면으로, 상단 히어로 헤더가 `고객 · 시작일 ~ 종료일`을 **맨 날짜 문자열로만** 출력해, 이 프로젝트가 마감에 얼마나 근접했는지·이미 지났는지를 헤더만 보고는 알 수 없었음(SPI 카드가 계획/실제 진척은 보여주지만 '잔여 며칠·초과 며칠'은 어디에도 없음). 배치79 /projects **목록**·배치90 /reports 표에 이미 들어간 D-day/초과 진단이 정작 그 프로젝트의 상세 화면에만 빠져 있던 누락. ① 종료일 기준 **D-day·초과 칩**을 날짜 구간 옆에 추가 — 상태가 진행(active)인데 종료일이 지났으면 흰 배경·빨강 `⚠ N일 초과`, 잔여 14일 이내는 흰 배경·주황 `D-N`, 그 밖은 반투명 흰 `D-N`(히어로 그라데이션 위 가독성 확보), 완료·보관 프로젝트는 톤다운(`종료`/`D-N`)해 과한 경보 억제. ② hover 툴팁에 `종료일 · 일정 경과 N% · 잔여/초과 일수` 산출 근거를 명시(경과율은 시작~종료 구간 기준). 종료일 미설정 프로젝트는 칩 미표시. 판정 규칙(active·±일수)은 배치79 목록과 동일하게 맞춰 두 화면 표기가 어긋나지 않게 함. 순수 클라이언트 계산·읽기 전용 표시라 데이터·API(project-summary)·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/projects/[id]/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~92가 목록·리포트·대시보드·관리 화면을 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 프로젝트 상세 대시보드 /projects/[id]**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_wk_1784441071). Edit 도구로 호스트 원본에 직접 기록 후 마운트(=OneDrive 실파일) 무결성 재확인 — 168줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0.

## 2026-07-19 (배치 92 — 배포 대기, 사용자·권한(/admin) 역할 한글 라벨·색상 통일 + 역할 구성 요약 KPI + 활성 관리자 0명 잠금 경고)
- ⑥/④ **사용자·권한(/admin): 한 번도 손대지 않은 관리 화면의 역할 표기 불일치·조직 잠금 사각지대 수정** — /admin은 배치 이력상 처음 손대는 화면으로, ① 역할 `<select>`가 저장 원문(admin/pmo/pm/member)을 **영문 그대로** 옵션으로 노출하고, ② 비관리자(읽기 전용) 뷰는 공용 `<Pill v={role}>`를 써서 전역 LABEL을 타는데 **거기엔 `pm='PM승인'`(테스트 검증단계 값)이 있어 역할 `pm`이 "PM승인"으로 오표기**되고 나머지(admin·pmo·member)는 라벨이 없어 영문으로 떨어지고 있었음(배지·타 화면과 어긋나는 표기 불일치, 배치18·79·81의 한글 라벨화 규칙 누락). ③ 화면 전체가 표 하나뿐이라 **역할 구성·활성/비활성 요약이 전무**했고, 결정적으로 **활성 관리자(admin)가 0명이 되면 역할 변경·스키마 업데이트 등 관리 기능이 통째로 잠기는데도** 그 위험이 어디에도 드러나지 않았음(운영 사고 1순위). ① 역할 라벨/색상을 **admin 화면 로컬로만** 정의(`ROLE_LABEL`·`ROLE_BADGE`)해 관리자용 select 옵션·읽기 전용 pill 모두 한글(관리자·PMO·PM·멤버)로 통일 — **전역 ui.tsx는 건드리지 않아** `pm='PM승인'`을 쓰는 테스트 화면에 영향 없음(안전 최우선). ② 상단 **요약 KPI 5장**(전체 구성원·활성·비활성·관리자·역할 구성 칩) 신설(kpis/kpi-label/kpi-value 표준 클래스 재사용) — 활성 관리자 0명이면 관리자 KPI를 빨강으로 부각. ③ **활성 관리자 0명 경고 배너**(빨강 좌측 바)를 표 위에 추가해 조직 잠금 위험을 즉시 색출. 읽기 전용 계산·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/admin/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~91이 도메인 목록·리포트·대시보드·백로그를 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 관리 화면 /admin**(사용자·권한)을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_wk_1784426577). 마운트(=OneDrive 실파일)에 python(encoding=\'utf-8\')으로 직접 기록 — 69줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0, 호스트 원본(Read) 꼬리 무결 재확인.

## 2026-07-19 (배치 91 — 배포 대기, 백로그·스프린트(/backlog) 요약 KPI 바 + 스토리포인트 '미추정' 색출)
- ⑧/③ **백로그 & 스프린트(/backlog): 한 번도 손대지 않은 스크럼 화면에 요약 KPI + 미추정(스토리포인트 0) 색출 추가** — /backlog는 배치 이력상 처음 손대는 화면으로, 스프린트별 진척 바(SprintProgress)는 있었지만 **포트폴리오 요약이 전무**했고, 결정적으로 **스토리포인트가 없는(0) 미완료 이슈**가 정상 이슈와 구분 없이 섞여 있어 — 포인트 미입력 이슈는 속도(velocity)·번다운·포인트 기준 진척 집계에서 조용히 빠지는데도(코드가 이미 포인트 없으면 건수 기준으로 폴백) 어디에도 드러나지 않았음(스크럼 감리 지적 1순위). ① 상단 **요약 KPI 5장** 신설(전체 이슈·백로그 대기·스프린트(진행 중)·잔여 포인트·미추정) — kpis/kpi 표준 클래스 재사용, 미추정은 0 초과 시 빨강. ② `unest()` 헬퍼로 각 **스프린트 헤더·백로그 헤더에 `⚠ 미추정 N` 주황 칩**을 붙여 어느 스프린트에 미추정 이슈가 몰려 있는지 즉시 색출(툴팁에 속도·번다운 영향 명시). ③ 각 이슈 행의 **스토리포인트 입력칸**을 미완료·0일 때 주황 테두리·강조 + 경고 툴팁으로 부각. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/backlog/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~90이 개별 목록·리포트·대시보드를 일순한 뒤, CHANGELOG에 **한 번도 등장하지 않은 유일한 화면 /backlog**(스크럼 백로그)를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784401502). OneDrive 마운트 동기화 지연으로 Edit 직후 마운트 사본 꼬리 잘림(100줄) → 호스트 원본(Read) 내용 기준으로 bash heredoc(quoted)로 마운트 사본을 동일 내용(140줄)으로 재구성, 깨진문자(U+FFFD) 0 재확인 후 tsc 통과.

## 2026-07-18 (배치 90 — 배포 대기, 리포트 '프로젝트별 현황' 표에 업무 진척·일정 경과율·일정 대비 지연(SV) 컬럼 추가)
- ③/⑥ **리포트(/reports): 포트폴리오 요약인 '프로젝트별 현황' 표가 정작 진척·일정 상태를 통째로 빼먹고 원시 날짜만 보여주던 누락 수정** — 리포트 맨 아래 '프로젝트별 현황' 표는 관리자가 전 프로젝트를 한눈에 훑는 자리인데도, 코드·프로젝트·고객·상태에 이어 기간을 **`시작일 ~ 종료일` 맨 문자열 한 컬럼으로만** 출력해, 각 프로젝트가 계획의 어디쯤 와 있는지(일정 경과)·업무 진척이 몇 %인지·일정 대비 지연인지를 전혀 알 수 없었음(배치79에서 /projects **목록**에 넣은 진척·일정 진단이 정작 종합 리포트 화면에는 빠져 있던 누락). ① **업무** 컬럼 신설: 이미 fetch된 tasks를 projectId로 묶어 `완료/전체` 건수를 표시하고, 프로젝트가 완료·보관 상태인데 미완료 업무가 남아 있으면 `⚠ 잔여 N`(빨강)으로 종료 처리만 하고 정리 안 된 프로젝트를 색출(/projects 목록과 동일 규칙). ② **진척** 컬럼 신설: 업무 평균 진척 바 + %에 더해, 진행 중 프로젝트가 **일정 경과율 대비 10%p 이상 뒤지면 `지연 N%p`(빨강 칩)** 을 병기 — 임계값·SV(진척−일정경과) 계산을 배치79 /projects·기성고 스냅샷 화면과 **동일한 ±10%p 규칙**으로 맞춰 세 화면의 지연 판정이 어긋나지 않게 함(툴팁에 산출 근거 명시). ③ **기간** 컬럼 재구성: 원시 두 날짜를 한 셀(`3.2~9.30`)로 합치고 **일정 경과율 바 + D-day**를 붙임 — 종료일이 지났는데 상태가 '진행'이면 `N일 초과 ⚠`(빨강), 잔여 14일 이내는 주황, 완료·보관 프로젝트는 회색 톤다운, 기간 미설정은 원본 날짜 그대로 표기. 헤더 colSpan을 5→7로 맞춤. 배치79의 `elapsed`/`d0` 헬퍼와 동일 로직을 리포트 모듈에 재구현(`elapsedOf`)해 규칙 일관성 유지. 이미 로드된 tasks·projects만 사용하는 순수 클라이언트 계산·읽기 전용 표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 단일 파일. — src/app/reports/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~89가 개별 목록 화면을 일순한 뒤, **한 번도 손대지 않은 종합 리포트 화면(/reports)에서 포트폴리오 표의 진척·일정 진단이 통째로 빠져 있던 결함성 누락**을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784379900). OneDrive 마운트 동기화 지연으로 Edit 직후 리포트 마운트 사본이 228줄에서 꼬리 잘림(통과율 표 tbody 이후 유실)·수분 대기 후에도 미해소 → 호스트 원본(Read) 내용 기준으로 통과율 헤더 앵커 이후를 호스트와 동일한 tail(라인229~260)로 재구성해 261줄 완성, 깨진문자(U+FFFD) 0·tsc 재검증 통과 확인. CHANGELOG는 마운트 사본 말미가 OneDrive 미완료 동기화로 잘려 python 재기록 대신 Edit 도구로 호스트 원본에 직접 기입.

## 2026-07-18 (배치 89 — 배포 대기, 대시보드 '마감 임박·결재 대기' 위젯에 이슈 기한(초과·임박) 반영)
- ⑥/③ **대시보드(/dashboard): 홈 '마감 임박·결재 대기' 위젯이 이슈 기한을 통째로 무시하던 누락 수정** — 대시보드는 "오늘 먼저 볼 것"을 모으는 허브인데도, 위젯이 **업무(endDate) 초과·임박과 산출물 결재 대기(review)만** 집계하고 **이슈(dueDate)는 아예 보지 않았음** — 이슈도 dueDate를 갖고 있고(schema) 전 목록 화면·/mywork·/calendar에는 배치79·80·88에서 기한 초과·임박 하이라이트가 이미 들어갔는데, 정작 가장 먼저 여는 대시보드에서만 빠져 있어 **마감을 넘긴 미해결 이슈가 홈 어디에도 뜨지 않던** 상태였음(결함성 누락). ① 위젯 계산에 **미해결 이슈(resolved·closed 제외)** 중 dueDate가 오늘 이전인 건은 `마감초과`(빨강), 7일 이내인 건은 `D-N`(주황)으로 업무와 동일 규칙·색상·태그로 추가하고, 클릭 시 `/issues`로 이동. 노출 순서는 **초과(업무·이슈) → 임박(업무·이슈) → 결재 대기**로 정렬해 급한 것이 위로 오게 함. ② 날짜 비교에 `vd()`(`Number.isFinite(new Date(s))`) 가드를 넣어 잘못된 날짜 문자열이 초과로 오판되지 않게 하고, 기존 업무 필터에도 동일 가드를 적용해 견고화. D-day 계산은 `dday()` 헬퍼로 통일. 순수 클라이언트 계산·표시라 데이터·API(/api/dashboard가 이미 이슈 전체를 반환)·스키마 무영향, 마이그레이션 불필요. 위젯 헤더·빈 상태 문구는 '마감 임박'이 이슈까지 포괄하므로 그대로 유지. 단일 파일. — src/app/dashboard/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~88의 폴리시를 이어, 전 목록 화면이 일순한 뒤에도 **정작 홈 대시보드 긴급 위젯에서 이슈 기한이 빠져 있던 결함성 누락**을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_wk_1784354543). OneDrive 마운트 동기화 지연으로 Edit 직후 마운트 사본 꼬리 잘림(196줄, 마지막 줄 중단) → 백업(무결)에 동일 치환을 적용해 python(encoding='utf-8')으로 마운트 사본을 호스트 원본과 동일 내용(206줄)으로 재구성, 깨진문자(U+FFFD) 0 재확인.

## 2026-07-18 (배치 88 — 배포 대기, 통합 캘린더(/calendar) '기한 초과' 하이라이트 + 상단 초과 요약 배지 + '+N' 오버플로 툴팁)
- ⑥/④ **통합 캘린더(/calendar): 전 목록 화면에 적용된 '기한 임박·초과 하이라이트' 규칙이 정작 마감이 모이는 캘린더에만 빠져 있던 문제 수정** — /calendar는 회의·업무마감·테스트/이슈 기한을 월력에 종류별 색상 칩으로 표시하면서도, **오늘 이전에 걸린 미완료 마감(=이미 지난 기한)을 미래 일정과 똑같은 색으로 그려**, 달력을 스캔해도 어떤 항목이 초과됐는지 알 수 없었음(마감이 한데 모이는 화면인데 정작 지연 색출이 안 됨). 배치79에서 /mywork에 넣은 것과 같은 누락. ① **칩 초과 강조**: 이벤트가 마감/기한 성격(업무마감·테스트기한·이슈기한, 회의는 '지남'일 뿐이라 제외)이고 그 날짜가 오늘보다 이전이면 빨강(`#c0392b`)·좌측 빨강 바(`inset 2px 0 0 #d64545`)·`⚠` 접두 + 툴팁에 '⚠ 기한 초과'를 붙여, 종료 안 된 업무(status!=='done')·미해결 이슈(resolved/closed 제외)만 이미 필터되어 들어오는 이벤트 중 지난 것을 그 자리에서 색출. ② **상단 요약 배지**: 표시 중(숨김 필터 제외)인 마감/기한 이벤트 중 오늘 이전 건을 전체 집계해 제목 옆에 `⚠ 기한 초과 N건`(빨강)을 노출 — /mywork의 크로스 도메인 초과 배지와 동일 취지로, 지금 보는 달(月)과 무관하게 프로젝트 전체의 초과 총량을 한눈에 알림(툴팁에 오늘 날짜·산출 근거 명시). ③ **'+N' 오버플로 툴팁**: 하루 4건 이상이라 `+N`으로 접힌 나머지 이벤트가 클릭·hover로도 확인 불가하던 것을, `+N`에 나머지 항목(`종류 · 제목`, 줄바꿈)을 hover 툴팁으로 붙여 접힌 일정도 볼 수 있게 함. 종류 필터 숨김(hidden) 상태는 배지·초과 집계 모두에 반영. 순수 클라이언트 계산·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 커스텀 월력 뷰(ResourceView 미사용) 단일 파일. — src/app/calendar/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~87의 폴리시를 이어, 전 목록 화면이 일순한 뒤에도 **한 번도 손대지 않은 채(파일 태그 편집 0건) 기한 초과 표시가 통째로 빠져 있던 통합 캘린더 /calendar**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_wk_1784340125). OneDrive 마운트 동기화 지연으로 Edit 직후 마운트 사본 꼬리 잘림(91줄, TS17008/1002/1005) → 호스트 원본(Read) 내용 기준으로 python(encoding='utf-8')으로 마운트 사본을 동일 내용(110줄)으로 재구성, 깨진문자(U+FFFD) 0 재확인.

## 2026-07-18 (배치 87 — 배포 대기, 기성고·스냅샷(/snapshots) '비고(note)' 미리보기 컬럼 + 기준일 미지정 명시)
- ⑥/③ **기성고·스냅샷(/snapshots): 폼에 입력만 받고 목록에 한 번도 노출되지 않던 '비고(note)'를 미리보기 컬럼으로 노출 + 기준일 누락 표기** — /snapshots는 계획·실적·괴리·기성률까지 진단하면서도, 정작 그 판단 근거인 **비고(note)에 무엇을 적었는지 목록에서 전혀 알 수 없어**(괴리·기성 사유·마일스톤 메모를 확인하려면 매번 상세를 열어야 함) 검수·감리 대응 시 근거 추적이 번거로웠음. ① **비고** 컬럼 신설: 목록 API가 이미 textarea 필드를 돌려주므로 추가 fetch 없이 `note`를 줄 단위로 파싱(글머리표 `- * · • ` 제거)해 `N줄`(테라코타) 칩 + 첫 줄 미리보기(한 줄 ellipsis) + 번호 매긴 hover 툴팁(앞 8줄)으로 표시 — boards·interfaces와 동일한 메모 미리보기 규칙. ② **기준일** 컬럼: 값이 없으면 회색 `미지정`(툴팁: 어느 시점의 진척·기성인지 알 수 없음)으로 명시하고 있으면 `ko-KR` 날짜로 포맷 — 기준 시점 없이 등록된 스냅샷(집계·추이에서 시점 불명)을 색출. ③ 비고 필드에 작성 가이드 hint, 도메인 맞춤 빈 상태(emptyText) 문구 추가. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/snapshots/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~86의 폴리시를 이어, 진단 카드는 갖췄으나 **정작 판단 근거인 비고가 목록에 없던 /snapshots**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784315083). 마운트(=OneDrive 실파일)에 python(encoding='utf-8')으로 직접 기록 — 85줄, 깨진문자(U+FFFD) 0 확인.

## 2026-07-17 (배치 86 — 배포 대기, 인력(/members) 이메일 형식·중복 등록 경고 + 연락처 자리수 검증)
- ⑥/④ **인력(/members): 통지·결재 알림의 수신처인 이메일·연락처가 목록에서 한 번도 검증되지 않던 문제 수정** — /members는 업무·부하·미해결이슈 집계까지 갖춘 화면인데도, 정작 **이메일·연락처는 맨 문자열로 출력만** 해서 오타난 주소("hong@gmail" 처럼 도메인 누락)나 여러 인력이 같은 이메일을 공유하는(계정 중복·오배정) 상태가 정상 값과 구분 없이 섞여 있었음 — 잘못된 주소는 알림·결재 통지가 조용히 유실되는 원인. ① **이메일** 컬럼을 render로 바꿔 형식 검증(간이 정규식) — 형식 오류는 주황 `형식 확인`, 빈 값은 muted `미입력`(통지 수신처 없음). ② **중복 등록 탐지**: 페이지-로컬 보조 fetch(`/api/members`, 읽기 전용, infra·firewall과 동일 패턴)로 동일 이메일을 쓰는 인력을 집계해 2명 이상이면 빨강 `⚠ 중복` + 툴팁에 충돌 인력 이름 목록. ③ **연락처** 컬럼: 숫자 자리수가 국내 번호 범위(9~11자리)를 벗어나면 주황 `자리수 확인`(입력 오류), 빈 값은 `미입력`. ④ 이메일 필드에 수신처 안내 hint, 연락처 필드에 형식 placeholder, 도메인 맞춤 빈 상태 문구 추가. 읽기 전용 파싱·집계·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/members/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~85의 폴리시를 이어, 부가 집계는 갖췄으나 **정작 이메일·연락처 정합성 검증이 전무하던 인력 명부 /members**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784293486). 마운트 사본에 bash heredoc로 직접 기록 — 126줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0 확인.

## 2026-07-17 (배치 85 — 배포 대기, 요구사항 추적(/rtm) 커버리지 상태 분포 요약 + 상태별 필터 칩)
- ③/⑤ **RTM(/rtm): 단일 '충족률 %'만 있던 매트릭스에 커버리지 상태 분포 요약 + 클릭 필터 신설** — /rtm은 요구사항별 커버리지(충족/진행중/이슈있음·테스트실패/미연계)를 행마다 표시하면서도 상단에는 충족률 하나뿐이라, **미연계가 몇 건인지·즉시 대응이 필요한 문제 요구사항이 몇 건인지**를 목록 전체를 스크롤하며 세어야 했음(요구사항이 수십 건이면 사실상 파악 불가). ① 각 행 판정 로직에 `bucket`(covered/progress/problem/unlinked)을 추가하고, 헤더 아래에 **상태 분포 요약 칩 5종**(전체·충족·진행중·이슈·실패·미연계)을 신설 — 각 칩에 건수·전체 대비 비율 툴팁을 붙여 "미연계 사각지대"와 "문제(이슈+테스트실패) 대응 대상"을 한눈에 노출. '문제'는 이슈 있음과 테스트 실패를 합산해 우선 대응군으로 묶음. ② 칩은 **클릭하면 해당 상태만 필터**(재클릭 해제)되고, 필터 시 `N건 표시(전체 M건)`와 빈 상태 문구를 분기. 순수 클라이언트 계산·표시라 API·데이터·스키마 무영향, 마이그레이션 불필요. 커스텀 테이블(ResourceView 미사용) 단일 파일. — src/app/rtm/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~84의 폴리시를 이어, 전 목록 화면이 일순한 뒤에도 **요약·필터가 전무하던 커스텀 테이블 화면 /rtm**을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784228789). OneDrive 마운트 동기화 지연으로 Edit 직후 마운트 사본 꼬리 잘림(77줄, TS17008/1002/1005) → bash heredoc로 마운트 사본을 호스트 원본과 동일 내용(111줄)으로 재기록해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-16 (배치 84 — 배포 대기, 업무 부하(/workload) 요약 KPI · 담당자 검색·카운트 필터 · 완료율·부하 라벨/툴팁)
- ③/⑧ **업무 부하(/workload): 한 번도 손대지 않은 35줄 최소 화면에 요약 KPI·검색·완료율·부하 라벨 추가** — /workload는 담당자·진행업무·미결이슈·완료·부하바만 나열해, 조직 전체가 얼마나 과부하인지 요약이 없고, 인원이 많아지면 특정 담당자를 스크롤로 찾아야 했으며, '완료'는 원시 건수만 있어 완료율(=진척 충실도)을 알 수 없었음. ① 상단에 **요약 KPI 4장**(집계 인원 · 과부하(최다 대비 70%↑) · 여유(부하 0) · 평균 부하) 신설 — 전체 담당자 기준 집계라 검색 필터와 무관하게 조직 상태를 표시(kpis/kpi 표준 클래스 재사용). ② **담당자 이름 검색창 + 카운트 필터**(`표시건/전체건`, 테라코타 강조 + 근거 툴팁 — 배치19·71·77과 동일 규칙) 추가, 검색 결과 없음 빈 상태 문구 분기. ③ **완료** 컬럼에 완료율(`완료건(백분율)`)과 `업무 N건 중 M건 완료` 툴팁을 붙여 원시 건수만으로는 알 수 없던 진척 충실도를 노출. ④ **부하** 바에 산출 근거 툴팁(`진행 업무 N · 미결 이슈 N · 완료 N — 부하 N건`)과 최다 담당자 `최다`(빨강)·부하 0 `여유`(초록) 라벨을 붙여 스캔 가독성 강화. 읽기 전용 표시·클라이언트 계산이라 데이터·API(/api/workload)·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/workload/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~83의 폴리시를 이어, 전 목록 화면 폴리시가 일순한 뒤에도 **한 번도 손대지 않은 채 남아 있던 요약·검색이 전무한 /workload(35줄)** 를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784207132). OneDrive 마운트 동기화 지연으로 Write 직후 마운트 사본 꼬리 잘림(40줄, TS17008/1002) 발생 → bash heredoc로 마운트 사본을 동일 내용(93줄)으로 재기록해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-16 (배치 83 — 배포 대기, 내 To-Do(/todos) 목록 '메모' 미리보기 컬럼 + 우선순위 한글 라벨·필드 hint·도메인 빈 상태)
- ⑥/⑧ **내 To-Do(/todos): 폼 입력만 받고 목록에 한 번도 노출되지 않던 '메모(note)'를 미리보기 컬럼으로 노출 + 표시 폴리시 통일** — /todos는 배치42에서 만든 뒤 손대지 않은 23줄짜리 최소 설정 화면으로, 코드·할 일·우선순위·기한·상태만 나열해 **메모(note)에 무엇을 적었는지 목록에서 전혀 알 수 없었음**(개인 홈 성격상 상세를 매번 열기 번거로움). ① **메모** 컬럼 신설: 목록 API가 이미 textarea 필드를 돌려주므로 추가 fetch 없이 `note`를 줄 단위로 파싱(글머리표 `- * · • 1.` 제거)해 `N줄`(테라코타) 칩 + 첫 줄 미리보기(한 줄 ellipsis) + 번호 매긴 hover 툴팁(앞 8줄)으로 표시 — 다른 화면(회의·게시판·리스크 등)과 동일한 메모 미리보기 규칙. ② **우선순위** select를 영문 원문(high/medium/low)에서 `{value,label}` 한글 라벨(높음·보통·낮음)로 교체해 배지 표기와 일관화(배치18·79·81과 동일 취지, 저장 값은 불변). ③ 메모·기한 필드에 작성 가이드 hint(한 줄에 하나씩 / 비우면 임박·초과 강조 제외) 추가, 도메인 맞춤 빈 상태 문구 적용. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/todos/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~82의 폴리시를 이어, 전 목록 화면 폴리시가 일순한 뒤에도 **정보 밀도가 가장 낮고 메모 본문이 목록에 없던 /todos(23줄)** 를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784142355). OneDrive 마운트 동기화 지연으로 첫 기록 후 꼬리 잔여 바이트(라인51 TS1127) 발생 → python(encoding='utf-8')으로 마운트 사본을 동일 내용(50줄)으로 재기록해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-15 (배치 82 — 배포 대기, 요구사항 목록 '완료조건(인수기준)' 컬럼 + 승인 단계인데 완료조건 미작성 경고 · 담당자 members 콤보 통일)
- ⑥/④ **요구사항(/requirements): 요구사항의 실질인 '완료조건(인수기준)'을 목록에 노출 + 미작성 색출** — /requirements는 이미 연계·커버리지·분포 분석까지 갖춘 화면인데도, 정작 요구사항의 본체인 **완료조건(acceptanceCriteria)·설명(description)이 목록에 전혀 없어**, "검증 기준 한 줄 없이 승인(approved)까지 올라간 요구사항"을 찾으려면 행을 하나씩 열어야 했음(검증 불가능한 승인 요구사항 = 감리·검수 지적 1순위). ① **완료조건** 컬럼 신설: 목록 API(crud)가 이미 textarea 필드를 돌려주므로 추가 fetch 없이 `acceptanceCriteria`·`description`을 줄 단위로 파싱(글머리표 `- * · • 1.` 제거)해 `조건 N`(초록) 칩 + 설명 첫 줄 미리보기(한 줄 ellipsis) + 번호 매긴 hover 툴팁(완료조건 앞 6건·설명 요약)으로 표시 — 상세를 열지 않고 요구사항 충실도를 판단. **상태가 검토(review)·승인(approved)인데 완료조건이 비어 있으면 `⚠ 완료조건 미작성`(빨강)** 으로 부각, 작성(draft) 단계에서는 `미작성`(muted)으로만 표기해 과한 경보를 만들지 않음(배치78 /tests '절차 미작성'·배치80 /issues '설명 없음'과 동일 규칙). ② **담당자** 필드를 자유 입력에서 `optionsFrom:'members'` 콤보로 바꿔 다른 도메인(업무·이슈·인터페이스 등)과 담당자 어휘 일치(배치73·75·76과 동일), 인수기준 필드에 작성 가이드 hint(검증 가능한 형태·승인 전 필수) 추가. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/requirements/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~81의 폴리시를 이어, 부가 분석은 갖췄으나 **정작 요구사항 본문(완료조건·설명)이 목록에 없던 /requirements**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784120718). 마운트(=OneDrive 실파일)에 python(encoding='utf-8')으로 직접 기록 — 142줄, 깨진문자(U+FFFD) 0 확인.

## 2026-07-15 (배치 81 — 배포 대기, 업무(WBS) 목록 '마감 D-day·초과' 하이라이트 + 선행 작업 정합성(없는 코드·자기참조·순서 위반) + 상태↔진척 모순 · 단계 화면 정합성 경고)
- ⑥/④ **업무(/tasks): 전 화면에 적용된 '기한 임박·초과 하이라이트' 규칙이 정작 핵심 도메인인 WBS 목록에만 빠져 있던 문제 수정 + 선행·진척 정합성 경고** — /tasks는 마감(endDate)을 **색상·강조 없는 맨 날짜 문자열**로 출력해, 지난 기한 업무가 정상 업무와 똑같은 회색 텍스트로 섞여 있었음(배치79에서 /mywork에 적용한 것과 같은 누락). ① **마감** 컬럼: 초과는 빨강 `⚠ N일 초과`, 7일 이내는 주황 `D-N`, 완료 업무는 톤다운 — 다른 목록 화면과 **동일한 규칙·색상**. 미완료인데 마감일 자체가 없으면 `기한 없음`(주황, 지연 집계·마감 알림·간트 막대에서 빠지는 업무). ② **선행** 컬럼: 페이지-로컬 보조 fetch(`/api/tasks?projectId=`, 읽기 전용, 배치75·76과 동일 패턴)로 프로젝트 업무를 코드 색인해 선행 작업을 실제로 조회 — 존재하지 않는 코드면 `⚠ 없는 코드`(주황, 간트 의존선·임계경로에서 조용히 무시되던 오타), 자기 자신을 가리키면 `⚠ 자기참조`(빨강), **선행이 완료되지 않았는데 이 작업이 진행중이면 주황·완료 처리됐으면 빨강 `⚠ 선행 미완료`**(순서 위반)로 표시하고 툴팁에 선행 작업명·상태·진척을 노출. 선행이 완료된 정상 건은 초록 `✓`. ③ **진척** 컬럼: 상태↔진척률 모순 3종 — 완료인데 진척 100% 미만(`⚠ 진척 미갱신`, 빨강 · EVM의 EV가 실제보다 낮게 잡히는 원인), 진척 100%인데 상태 미완료(`완료 처리 필요`, 주황), 진행중인데 진척 0%(`미착수`). ④ **담당**: 진행중인데 미지정이면 `⚠ 미지정`(빨강, 부하 집계·알림 대상에서 제외), **단계**: 착수 이후인데 미지정이면 주황(간트 스와이레인·단계별 집계 제외). ⑤ **공수**: 완료됐는데 실제 공수가 비면 `공수 미기록`(주황, AC·CPI 집계 누락) 경고 추가(기존 소진율 바·툴팁은 유지). ⑥ 상태 select를 영문 원문(todo/doing/done)에서 한글 라벨(할 일·진행중·완료)로 교체해 배지와 일관화(배치18·79와 동일 취지), 단계·담당자·마감일·선행·진척률 필드에 hint 보강, 도메인 맞춤 빈 상태 문구 적용. 읽기 전용 조회·표시라 데이터·API·스키마 무영향. — src/app/tasks/page.tsx
- ⑥/④ **단계(/phases): 단계 상태 ↔ 하위 업무 정합성 경고 + 상태 한글 라벨** — 단계 화면은 업무 건수·평균 진척을 보여주면서도 **단계 상태와 실제 업무 상태의 모순을 전혀 검사하지 않았음**. ① **업무** 컬럼: 단계가 완료인데 미완료 업무가 남아 있으면 `⚠ 잔여 N`(빨강, /projects의 동일 규칙과 일치), 단계가 진행·완료인데 묶인 업무가 0건이면 `⚠ 업무 없음`(빨강) — 업무의 '단계' 필드 값이 단계명과 정확히 일치해야 매칭되는 규칙 때문에 오타로 통째로 누락된 단계를 색출(툴팁에 매칭 규칙 안내). ② **진척** 컬럼: 단계 완료인데 평균 진척 100% 미만이면 `⚠ 진척 미갱신`, 단계가 '계획'인데 이미 착수·완료된 업무가 있으면 `착수됨`(주황). ③ 상태 select 한글 라벨(계획·진행·완료), 단계명 필드 hint에 매칭 규칙 명시, 빈 상태 문구 추가. — src/app/phases/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~80의 보너스 폴리시를 이어, 전 화면 폴리시가 일순한 뒤 **정작 한 번도 손대지 않은 채 남아 있던 최핵심 도메인 /tasks(19줄 설정 파일)** 와 그 짝인 **/phases**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784055893). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(tasks 73줄·phases 62줄) → 호스트 원본 내용 기준으로 python(encoding='utf-8')으로 마운트 사본을 동일 내용(tasks 157줄·phases 85줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-14 (배치 80 — 배포 대기, 이슈 목록 '설명(재현정보)·연계(요구사항/관계이슈)·공수 소진율' 컬럼 + 담당·기한 누락 경고 · 테스트 차수 '기간·정합성' 경고)
- ⑥/④ **이슈·결함(/issues): 입력만 받고 목록에 한 번도 보여주지 않던 4개 값(description·reqCode·related·estimateHours)을 컬럼으로 노출 + 정합성 경고** — /issues는 핵심 도메인인데도 목록이 코드·제목·유형·우선순위·상태·기한·에픽·SP·실제공수·라벨·담당 나열에 그쳐, **결함의 본체인 설명(재현 절차·현상)이 채워졌는지 목록에서 알 수 없었고**(설명 한 줄 없이 '해결'로 종결된 결함을 찾으려면 행을 하나씩 열어야 함), **연계 요구사항(reqCode)·관계 이슈(related)는 폼에 입력 필드가 있는데도 목록에 아예 표시되지 않았으며**, 공수는 `실제공수` 원시 숫자만 있고 **예상공수(estimateHours) 대비 소진율은 업무(/tasks)에만 있고 이슈에는 없었음**. ① **설명** 컬럼: `description`을 줄 단위로 파싱(글머리표 제거)해 `N줄` 칩 + 첫 줄 미리보기(한 줄 ellipsis) + 번호 매긴 hover 툴팁(앞 8줄). **유형이 결함(bug)인데 열림(open)을 벗어난 상태에서 설명이 비면 `⚠ 설명 없음`(빨강)** — 재현·검증이 불가능한 결함(반려 1순위)을 목록 스캔만으로 색출하고, 해결·종료인데 내용이 없으면 `설명 없음`(주황), 작성 초기(open)에는 `미작성`(muted)만 표기해 과한 경보를 만들지 않음. ② **연계** 컬럼 신설: `reqCode`(테라코타 monospace 칩)와 `related`(관계 이슈 코드 칩, 3건 이상은 `외 N` 축약 + 전체 툴팁)를 함께 노출하고, **결함·변경요청인데 둘 다 비면 `⚠ 미연계`(주황)** — RTM(요구사항 추적) 커버리지 집계에서 통째로 빠지는 이슈를 드러냄(배치78의 /tests `⚠ 미연계`와 동일 규칙). ③ **공수(h)** 컬럼: 실제/예상 `6 / 8h` 표기 + 소진율 바(80%↑ 주황 · 초과 빨강 `⚠`) + 산출 근거 툴팁으로 재구성 — **업무(tasks) 공수 셀과 완전히 동일한 규칙·색상**을 적용해 두 화면의 공수 판정이 어긋나지 않게 했고, 해결·종료됐는데 실제공수가 없으면 `공수 미기록`(주황)으로 원가(AC)·CPI 집계 누락을 경고. ④ **담당** 컬럼: 진행중인데 담당자가 없으면 `⚠ 미지정`(빨강, 아무도 손대지 않는 이슈), 미해결 상태의 미지정은 `미지정`(주황, 부하 집계 제외)으로 구분. **기한** 컬럼: 기존 초과·임박 하이라이트를 유지하되 **미해결인데 기한 자체가 없으면 `기한 없음`(주황)** 을 표기(마감 알림·지연 집계에서 제외되는 이슈), 해결·종료 건은 톤다운해 경보 해제. ⑤ 설명·예상공수 필드에 작성 가이드 hint·placeholder 추가, 도메인 맞춤 빈 상태 문구 적용. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/issues/page.tsx
- ⑤/④ **테스트 차수(/test-cycles): '목표·범위' 본문 컬럼 + '기간'(경과율·초과일수) + 차수↔케이스 정합성 경고** — 배치33에서 만든 차수 화면은 목표·시작·종료를 **맨 문자열로 나열만** 해, 차수의 실질인 목표·범위(goal, 검증 대상·합격 기준)가 채워졌는지 알 수 없었고, **종료일이 지났는데 여전히 '진행'인 차수**나 **완료 처리했는데 미실행 케이스가 남은 차수**(= 검증하지 않고 닫은 회차, 감리 지적 1순위)가 전혀 드러나지 않았음. ① **목표·범위** 컬럼: 줄 단위 파싱 + `N줄` 칩·첫 줄 미리보기·번호 매긴 툴팁, 진행·완료 차수인데 비어 있으면 `⚠ 범위 미작성`(빨강). ② **기간** 컬럼: 시작~종료를 한 셀(`7.1~7.20`)로 합치고 **일정 경과율 바**(배치79 /projects와 동일 규칙)와 D-day를 붙임 — 종료일이 지났는데 완료가 아니면 `N일 초과 ⚠`(빨강), 7일 이내면 `D-N`(주황), 완료 차수는 회색 톤다운, 기간 미설정은 별도 표기. ③ **케이스** 컬럼: 진행·완료인데 이 차수로 묶인 테스트가 0건이면 `⚠ 케이스 없음`(빨강, 차수만 만들고 테스트의 '차수' 필드를 지정하지 않은 상태), **완료 차수에 미실행 케이스가 남아 있으면 `⚠ 미실행 N`**(빨강). ④ **통과율** 컬럼: 실패 건수 `⚠N`(빨강)·블록 건수 `■N`(주황) 칩을 병기하고 통과율이 **실행된 케이스 기준**임을 툴팁에 명시, 케이스는 있는데 전부 미실행인 진행·완료 차수는 `⚠ 미실행`(빨강)으로 부각. ⑤ 차수명·목표·종료일 필드에 hint(테스트의 '차수' 필드에 같은 이름을 넣어야 묶인다는 매칭 규칙 안내 포함), 도메인 맞춤 빈 상태 문구 추가. 읽기 전용 집계·표시라 데이터·API·스키마 무영향. — src/app/test-cycles/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~79의 보너스 폴리시를 이어, 전 화면 1차 폴리시가 일순한 뒤 **가장 자주 쓰이면서도 정작 폼 입력값의 절반이 목록에 노출되지 않던 핵심 도메인 /issues**와, 배치33 이후 손대지 않은 **/test-cycles**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1784034291). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(issues 75줄·test-cycles 71줄, 30초 대기 후에도 미해소) → 호스트 원본(Write) 내용 기준으로 마운트 사본을 동일 내용(issues 257줄·test-cycles 137줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-14 (배치 79 — 배포 대기, 프로젝트 목록 '기간·일정 경과율' + 계획 대비 지연 진단 · 내 작업(mywork) 기한 초과 하이라이트·임박순 정렬)
- ⑥/③ **프로젝트 목록(/projects): '기간' 컬럼(일정 경과율 바 + D-day) 신설 + 일정 대비 진척 지연 진단 + 계약금액·종료 정합성 경고** — /projects는 시작일·종료일을 **날짜 문자열 두 컬럼으로 나열만** 해서, 프로젝트가 기간의 어디쯤 와 있는지(=계획 진척)를 알 수 없었고, 그 결과 **평균 진척 40%가 일정 20% 시점의 40%인지 90% 시점의 40%인지 구분되지 않아** 목록만 보고는 지연 프로젝트를 전혀 골라낼 수 없었음(포트폴리오 화면의 존재 이유가 죽어 있던 셈). ① **기간** 컬럼: 시작~종료 두 컬럼을 한 셀(`3.2~9.30`)로 합치고 **일정 경과율 바**(오늘이 구간의 몇 %인지 = EVM의 PV 근사)와 D-day 칩을 붙임 — 잔여 14일 이하 주황, **종료일이 지났는데 상태가 `active`면 `N일 초과 ⚠`(빨강)** 로 "끝난 줄 알았는데 안 끝난 프로젝트"를 색출하고 툴팁에 종료 처리·기간 연장 안내를 제공. 완료·보관 프로젝트는 회색으로 톤다운해 과한 경보를 만들지 않고, 기간 미설정은 `기간 미설정`으로 구분 표기(경과율 계산 불가 = 지연 판정에서 제외되는 프로젝트). ② **진척** 컬럼: 기존 평균 진척 바에 **일정 경과율 대비 괴리(SV %p)** 를 더해, 진행 중 프로젝트가 일정보다 **10%p 이상 뒤지면 `지연 N%p`(빨강 칩)** 을 병기 — 임계값은 기성고 스냅샷 화면(`gapCell`)의 ±10%p 규칙과 **동일하게 맞춰** 두 화면의 지연 판정이 어긋나지 않게 함. 툴팁에 `업무 24건 평균 진척 41% / 일정 경과 63% (계획 진척 근사) / 일정 대비 22%p 지연` 산출 근거를 명시. ③ **업무** 컬럼: 프로젝트가 **완료·보관 상태인데 미완료 업무가 남아 있으면 `⚠ 잔여 N`**(빨강) — 종료 처리만 하고 하위 업무를 정리하지 않은 프로젝트(검수·정산 시 잔여 WBS 지적 항목)를 목록 스캔만으로 색출. 종료 프로젝트에서는 기한 초과 경보를 해제해 이중 경고를 피함. ④ **계약금액**: 원 단위 원문 대신 `8.5억원`·`3,200만원`으로 축약(정확한 금액은 툴팁, tabular-nums 정렬)하고, **진행 중인데 계약금액이 비면 `⚠ 미입력`**(주황) — 기성·정산 집계의 분모가 없는 프로젝트를 드러냄. **고객** 미지정도 진행 중 프로젝트에 한해 주황 표기. ⑤ 상태 select를 영문 원문(active/completed/archived)에서 한글 라벨(진행·완료·보관)로 교체해 배지 표기와 일관화(배치18과 동일 취지), 계약금액·시작/종료일·고객 필드에 작성 가이드 hint·placeholder 추가, 도메인 맞춤 빈 상태 문구 적용. 순수 계산·읽기 전용 표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/projects/page.tsx
- ④/② **내 작업(/mywork): 기한 초과·임박 하이라이트 + 기한 임박순 정렬 + 지연 건수 배지** — /mywork는 매일 여는 개인 홈 화면인데도 **기한(마감·dueDate)을 색상·강조 없는 맨 날짜 문자열로 출력**하고 있어, 전 목록 화면에 적용된 ⑥ 항목의 '기한 임박/초과 하이라이트' 규칙이 **정작 가장 자주 보는 화면에만 빠져 있었음** — 지난 기한 업무가 정상 항목과 똑같은 회색 텍스트로 섞여 있고, 정렬도 API 반환 순서(사실상 등록순)라 마감이 코앞인 항목이 목록 맨 아래에 묻히는 일이 흔했음. ① 4개 섹션(To-Do·업무·이슈·리스크) 전부에 **공통 `dueCell` 렌더러**를 적용 — 초과는 빨강 `⚠` + `N일 기한 초과` 툴팁, 7일 이내는 주황 `D-N`, 완료(done·resolved·closed) 항목은 경보 해제 후 톤다운(다른 목록 화면과 **동일한 규칙·색상**). 이슈·리스크 섹션에는 아예 없던 **기한 컬럼을 신설**(API가 이미 dueDate를 반환하고 있었음). ② 각 섹션을 **기한 임박순으로 정렬**(기한 없는 항목은 뒤로, 완료 항목은 맨 뒤로) — 지난 기한 항목이 자동으로 맨 위에 오고, 완료 행은 opacity 0.62로 톤다운. ③ 섹션 헤더에 **`지연 N건`** 빨강 배지를, 화면 상단에 **`⚠ 기한 초과 N건`**(4개 도메인 합산) 배지를 붙여 "오늘 먼저 볼 것"을 한눈에 노출. ④ 섹션별 빈 상태 문구를 공통 "배정된 항목이 없습니다"에서 도메인 맞춤 문구로 분리(⑥ 공통 항목 규칙). 클라이언트 표시·정렬만 변경이라 API·데이터·스키마 무영향. — src/app/mywork/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~78의 보너스 폴리시를 이어, 전 목록 화면 1차 폴리시가 일순한 뒤 **포트폴리오 최상위 화면(/projects)** 과 **매일 여는 개인 홈(/mywork)** — 두 화면 모두 기한·일정 축 표시가 통째로 빠져 있던 곳 — 을 선택(신규 테이블 미접촉). ROADMAP 변경 없음(체크할 안전 `[ ]` 항목 없음).
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783969481). 두 파일 모두 python(encoding='utf-8')으로 마운트에 직접 기록 — 121줄·110줄, 꼬리 잘림 없음·깨진문자(U+FFFD) 0 확인.

## 2026-07-13 (배치 78 — 배포 대기, 테스트 목록 '절차·기대' 본문 컬럼 + 결과↔검증단계 모순(미실행/실패 완료) 경고 · RTM 미연계 색출 · 산출물 결재 증빙 경고)
- ⑤/④ **테스트(/tests): 테스트 케이스의 본체(절차·기대 결과)를 목록에 노출 + 결과·검증단계·진척의 모순 색출** — /tests는 코드·제목·유형·요구사항·차수·담당·기한·진척·결과·검증단계를 나열하면서도 **테스트 케이스의 실질인 절차(steps)·기대 결과(expected)가 목록에 전혀 없어**, "절차 한 줄 없이 PM 승인까지 올라간 케이스"를 찾으려면 행을 하나씩 열어야 했음(무엇을 어떻게 검증했는지 증빙이 없는 상태 = 감리 지적 1순위). ① **절차·기대** 컬럼 신설: 목록 API가 이미 steps·expected를 돌려주므로 추가 fetch 없이 줄 단위 파싱(글머리표 `- * · • 1.` 제거)해 `절차 5`(테라코타)·`기대 3`(초록) 칩 + 절차 첫 줄 미리보기(한 줄 ellipsis)를 표시하고, hover 툴팁에 번호 매긴 절차 앞 6단계·기대 결과 앞 3건을 노출 — 상세를 열지 않고 케이스 충실도를 판단. **검증 단계가 개발자검증(dev) 이상인데 절차가 비어 있으면 `⚠ 절차 미작성`(빨강)**, 절차는 있으나 기대 결과가 없으면 `기대결과 없음`(주황)을 병기하고, 작성(draft) 단계에서는 `미작성`(muted)으로만 표기해 과한 경보를 만들지 않음. ② **결과** 컬럼을 단순 배지에서 render로 바꿔 **결과↔검증단계↔진척의 모순**을 4종 경고로 검사 — 검증 완료인데 결과가 미실행(`⚠ 미실행 완료`, 빨강), 실패한 테스트가 완료로 종결(`⚠ 실패 완료`, 빨강 — 결함 조치 없이 닫힌 케이스), 블록 상태로 완료(`⚠ 블록 완료`), 통과인데 진척률 100% 미만(`진척 미갱신`, 주황), 실패 건에 보고자 미지정(`보고자 없음`, 주황). 각 경고는 조치 안내를 툴팁으로 제공. ③ **요구사항** 컬럼: 연계 요구사항(reqCode)이 비면 `⚠ 미연계`(주황) — **RTM 커버리지 집계에서 통째로 빠지는 케이스**를 목록 스캔만으로 색출(배치의 RTM 커버리지 연계와 짝을 이룸), 코드가 있으면 monospace 표기. ④ 절차·기대 결과 필드에 작성 가이드 hint(한 줄에 단계/검증항목 하나씩)를 넣어 입력 시점부터 줄 단위 작성을 유도하고, reqCode 필드 hint에 커버리지 제외 경고를 명시, 도메인 맞춤 빈 상태 문구 추가. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/tests/page.tsx
- ⑥/④ **산출물·결재(/documents): 결재 증빙 누락 + 결재 대기 체류일 경고** — 목록에 결재자·결재일 컬럼이 있는데도 **값이 비면 그냥 빈 칸**이라, `승인` 상태인데 결재자나 결재일이 없는(=누가 언제 승인했는지 추적 불가) 산출물이 정상 승인 건과 구분 없이 섞여 있었고, 결재요청 상태로 몇 주째 방치된 문서도 드러나지 않았음. ① **결재자**: 승인·반려 상태인데 결재자가 없으면 `⚠ 결재자 없음`(빨강) — 결재 이력 증빙 누락(감리·검수 지적 항목). ② **결재일**: 승인인데 결재일이 없으면 `⚠ 결재일 없음`(빨강). 결재요청 상태에서는 **등록일 기준 대기 일수**를 계산해 7일 이상이면 `대기 N일`(주황), 14일 이상이면 빨강 `⚠`으로 부각하고, 결재요청 시각을 별도로 저장하지 않으므로 **근사치임을 툴팁에 명시**(정확한 요청 시각 기록은 스키마 변경이 필요해 야간 범위 밖). ③ **유형**: 미지정이면 `미지정`(주황) — 표준 양식(form_definitions) 매칭·유형별 집계(배치70)에서 제외되는 문서를 색출. **작성**: 결재 라인에 올라갔는데 작성자가 없으면 `미지정`(주황). 유형·결재자 필드에 hint 추가, 도메인 맞춤 빈 상태 문구 적용. 읽기 전용 표시라 데이터·API·스키마 무영향. — src/app/documents/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~77의 보너스 폴리시를 이어, 전 화면 1차 폴리시가 일순한 뒤 **핵심 도메인이면서도 정작 케이스 본문이 목록에 없던 /tests**와 **결재 증빙 검증이 빠져 있던 /documents**를 선택(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783947881). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(tests 51줄·documents 49줄, 25초 대기 후에도 미해소) → 호스트 원본 내용 기준으로 python(encoding='utf-8')으로 마운트 사본을 동일 내용(tests 103줄·documents 84줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-13 (배치 77 — 배포 대기, 알림 화면 **link 미사용 결함 수정**(행 클릭 이동) + 유형 분류·안읽음 필터·검색·상대시각 · 게시판 본문 미리보기)
- ④ **[결함 수정] 알림(/notifications)의 `link` 필드가 UI에서 한 번도 쓰이지 않던 문제** — `notifications` 테이블은 `link` 컬럼을 갖고 있고 자동 생성 라우트(`/api/notifications/generate`)가 마감 초과·마감 임박에는 `/tasks`, 결재 대기에는 `/documents`를 **실제로 채워 넣고 있는데도**, 알림 화면은 message·isRead·createdAt만 렌더링해 **링크를 완전히 버리고 있었음** — 사용자는 "결재 대기: DOC-12" 알림을 보고도 산출물 화면으로 직접 이동할 수 없어 좌측 메뉴를 다시 눌러 찾아가야 했음(알림의 존재 이유인 '바로 가기'가 죽어 있던 셈). 행 클릭 시 `router.push(link)`로 이동하도록 연결하되 **내부 경로(`/`로 시작)만 허용**해 외부 링크 주입을 차단하고, 키보드 접근성(tabIndex·Enter/Space)과 이동 대상 툴팁을 함께 제공. 링크 없는 알림은 커서·탭포커스를 주지 않아 클릭 가능 여부가 시각적으로 구분됨.
- ⑥/⑧ **알림 화면 재구성 — 유형 분류·필터·검색·상대시각·스켈레톤** — 기존 화면은 필터도 검색도 없이 최근 50건을 `[자동] 마감 초과: TSK-03 …` **원문 그대로** 한 줄씩 나열하기만 해, 읽지 않은 알림만 골라 보거나 결재 대기만 추려 보는 기본 동선이 없었음. ① 메시지를 파싱해 **유형 컬럼**(마감 초과 빨강 · 마감 임박 주황 · 결재 대기 테라코타 · 일반 회색 배지)으로 분리하고 본문에서 `[자동] 마감 초과: ` 접두어를 걷어내 제목만 남김(자동 생성분은 `자동` 표식 + 툴팁으로 보존). ② **검색창** + **유형 필터**(옵션에 건수 표기 — `결재 대기 (3)`) + **안 읽음만** 토글 버튼을 툴바에 추가하고, 배치19·71과 동일한 `표시건/전체건` 카운트(테라코타 강조 + 근거 툴팁)를 붙임. ③ 시각을 `방금 전 / N분 전 / N시간 전 / N일 전`(30일 초과는 날짜) **상대시각**으로 바꾸고 절대시각은 툴팁으로 이동 — 알림의 신선도를 즉시 스캔. ④ 헤더에 `N건 읽지 않음`을 표기하고 '모두 읽음' 버튼은 안 읽은 알림이 0건이면 비활성화, 읽은 알림은 opacity 0.72로 톤다운. ⑤ 로딩 스켈레톤(.skel 4행)을 넣어 ④ 항목의 로딩 일관화 규칙에 맞추고, 빈 상태를 **알림 자체가 없음** vs **필터 결과 없음**으로 분기. — src/app/notifications/page.tsx
- ⑥ **게시판(/boards): 본문 미리보기 컬럼 + 작성일 상대시각·NEW 배지** — /boards는 5줄짜리 순수 설정 화면으로 번호·분류·제목·작성자·작성일만 있어 **본문(content)이 채워졌는지조차 목록에서 알 수 없었음**. `content`를 줄 단위로 파싱해 `N줄` 칩 + 첫 줄 미리보기(한 줄 ellipsis) + 번호 매긴 hover 툴팁(앞 8줄)으로 노출하고, **본문이 비면 `⚠ 본문 없음`(빨강)** 으로 제목만 있는 껍데기 글을 색출. 작성일을 상대시각(절대시각은 툴팁)으로 바꾸고 3일 이내 글에 `NEW`(초록) 배지를 붙여 새 공지를 스캔 가능하게 함. 작성자 필드를 `optionsFrom:'members'` 콤보로 바꿔 다른 도메인과 담당자 어휘 일치(배치73·75와 동일), 본문 필드에 작성 가이드 hint, 도메인 맞춤 빈 상태 문구 추가. — src/app/boards/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~76의 보너스 폴리시를 이어 **마지막까지 손대지 않은 두 화면(/notifications·/boards)** 을 선택했고, 그 과정에서 표시 개선이 아닌 **실제 기능 결함(link 미사용)** 을 발견해 ④ 관점으로 함께 수정함(신규 테이블 미접촉). 이로써 전 목록 화면의 1차 폴리시가 완전히 일순함. ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783897538). 이번 실행은 마운트 사본에 python(encoding='utf-8')으로 직접 기록해 꼬리 잘림 없음(175줄·52줄, 깨진문자 U+FFFD 0 확인). 클라이언트 표시·라우팅 변경뿐이라 API·데이터·스키마 무영향, 마이그레이션 불필요.

## 2026-07-13 (배치 76 — 배포 대기, 방화벽 신청 '사유·승인' 증빙 경고 + any/광범위 오픈·중복 정책 탐지 · 만료 정책 회수 알림)
- ⑥/④ **방화벽 신청(/firewall): 보안 검증 컬럼 재구성 — 사유·승인자 증빙 누락, 전체대역(any)·광범위 포트, 중복 정책, 만료 후 미회수 탐지** — /firewall은 출발지·목적지·포트·프로토콜을 **맨 문자열로 나열만** 해서, 방화벽 정책의 실질 증빙인 **사유(reason)·승인자(approver)가 목록에 아예 없었고**(승인은 났는데 사유 칸이 빈 신청을 찾으려면 행을 하나씩 열어야 함), `any`/`0.0.0.0/0` 같은 **전체 대역 오픈이나 `1-65535` 전체 포트가 아무 표시 없이 통과**됐으며, 만료일이 지났는데도 상태가 `approved`로 남은 **미회수 정책**과 동일 정책 **중복 신청**도 드러나지 않았음(전부 보안심사·감리 지적 항목). ① **출발지 → 목적지** 컬럼: 두 IP를 한 셀로 합쳐 `10.0.1.20 → 10.0.2.30`(monospace)로 보여주고, `any`·`*`·`0.0.0.0/0` 등 전체 대역이면 `⚠` 빨강 + "필요한 대역으로 좁히세요" 툴팁을, IPv4·CIDR 형식이 아니면 `형식 확인`(주황)으로 오타를 색출. IP 미입력은 `미지정`(주황). ② **포트·프로토콜** 컬럼: 단일(443)·범위(8000-8100)·목록(80,443) 표기를 파싱해 허용 포트 개수를 계산 — 전체 포트(any·1-65535)는 빨강 `⚠`, 형식 오류는 `형식 확인`, **허용 포트 100개 초과는 주황 경고**(툴팁에 `허용 포트 8,192개 — 범위가 넓습니다`)로 광범위 오픈을 부각. 승인 상태인데 포트가 비면 `⚠ 포트 미지정`(허용 범위 불명 = 심사 지적). 프로토콜은 테라코타 칩으로 병기. ③ **사유·승인** 컬럼 신설: `reason`(textarea, 목록 API가 이미 반환)을 줄 단위로 파싱해 첫 줄 미리보기 + 번호 매긴 hover 툴팁(앞 6줄)으로 노출하고, **승인·완료 상태인데 사유가 비어 있으면 `⚠ 사유 미작성` 빨강**, 승인자가 비어 있으면 `승인자 없음`(주황)을 병기 — 증빙 없는 승인 정책을 목록 스캔만으로 색출. 승인자가 있으면 `승인 홍길동`(초록)으로 결재 이력을 표시. 작성 단계(requested)에서는 muted 표기로만 두어 과한 경보를 만들지 않음. ④ **만료** 컬럼: 기존 D-day·초과 하이라이트를 유지하되, **만료일이 지났는데 상태가 아직 `approved`면 `회수 필요`(빨강) 칩**을 붙여 "만료됐지만 실제로는 열려 있는 정책"을 부각하고, 승인 정책에 만료일 자체가 없으면 `기한 없음`(주황)으로 영구 오픈을 경고. ⑤ **중복 정책 탐지**: 페이지-로컬 보조 fetch(`/api/firewall?projectId=`, 읽기 전용, 배치60·70·75와 동일 패턴)로 `출발지|목적지|포트|프로토콜` 키를 집계해 같은 정책이 2건 이상이면 제목 옆에 `⚠ 중복` + 툴팁에 충돌 신청 제목 목록(반려 건은 판정 제외). ⑥ 승인자 필드를 자유 입력에서 `optionsFrom:'members'` 콤보로 바꿔 다른 도메인과 담당자 어휘 일치(배치73·75와 동일), IP·포트·사유·만료일 필드에 작성 가이드 hint·placeholder 추가, 도메인 맞춤 빈 상태 문구 적용. 읽기 전용 파싱·집계·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/firewall/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~75의 보너스 폴리시를 이어, **마지막까지 한 번도 손대지 않은 7줄짜리 순수 ResourceView 설정 화면 /firewall** 을 선택(신규 테이블 미접촉). 이로써 전 목록 화면의 1차 가독성·정합성 폴리시가 일순함. ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783890337). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(31줄, TS1123) → 호스트 원본(Write) 내용 기준으로 python(encoding='utf-8')으로 마운트 사본을 동일 내용(153줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-13 (배치 75 — 배포 대기, 조달 '금액 비중·발주/입고 정합성' 경고 + 인프라 CMDB 'IP·호스트명 중복' 탐지)
- ⑥/④ **조달(/procurement): 금액 비중 바 + 발주·입고 정합성 경고** — /procurement는 수량·단가·금액을 숫자로만 나열해 **어떤 품목이 예산을 크게 먹는지 비교가 불가**했고, 더 큰 문제로 **상태만 `입고`로 바꿔놓고 발주번호(PO)·입고일 같은 증빙을 비워둔 행이 목록에서 전혀 드러나지 않았음**(정산·감리 지적 1순위). ① **금액** 컬럼: `수량×단가` 산출값에 **프로젝트 조달 총액 대비 비중 바 + %** 를 붙이고(페이지-로컬 보조 fetch로 총액 집계, 배치60·70과 동일 읽기 전용 패턴), 툴팁에 `수량 2 × 단가 3,500,000원 = 7,000,000원 / 조달 총액 중 18.4%` 산출 근거를 명시. 발주 이후 단계(ordered·received·completed)인데 수량·단가가 비어 금액을 낼 수 없으면 `⚠ 금액 미산정`(빨강) — **예산 집계에서 통째로 빠지는 품목**을 색출. 작성 단계(requested)에서는 `미산정`(muted)으로만 표기해 과한 경고를 만들지 않음. ② **발주·입고** 컬럼 신설: 발주번호 칩(테라코타)·입고일 칩(초록)을 표시하고 상태와의 모순을 3종 경고로 검사 — 발주 이후인데 PO 없음(`⚠ 발주번호 없음`), 입고·완료인데 입고일 없음(`⚠ 입고일 없음`), 입고일은 있는데 상태 미갱신(`상태 미갱신`, 주황). ③ 업체 미지정(발주 이후) 경고, 수량·단가 tabular-nums 정렬, 납기 셀은 입고 완료 시 경고 해제·톤다운, 폼에 PO/입고일/단가 작성 hint·placeholder 추가. — src/app/procurement/page.tsx
- ⑥/④ **인프라 자산(/infra): IP·호스트명 중복 탐지 + 위치·사양 압축** — CMDB인데 **같은 IP를 쓰는 자산이 두 건 등록돼도 목록에서 알 수 없어** IP 충돌·중복 등록이 그대로 방치됐음. ① **IP** 컬럼: 페이지-로컬 보조 fetch로 프로젝트 내 IP/호스트명을 집계해 중복이면 `⚠ 중복`(빨강) + 툴팁에 충돌 자산명 목록을 노출(폐기 자산은 판정 제외). IPv4 형식이 아니면 `형식 확인`(주황)으로 오타·CIDR 혼입을 알림. 호스트명도 동일 규칙으로 중복 표시. ② **위치·랙**을 한 셀로 합치고(랙은 칩), **사양**(OS·CPU·메모리)을 한 셀로 압축해 툴팁에 전체 노출 — 10개였던 컬럼의 폭 낭비를 줄이면서 정보량은 늘림. ③ 운영 자산인데 담당자 미지정이면 `미지정`(주황) 경고, 담당자 필드를 `optionsFrom:'members'` 콤보로 바꿔 다른 도메인과 담당자 어휘 일치(배치73과 동일), IP·랙·사양 필드에 placeholder·hint 추가. 읽기 전용 집계·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉. — src/app/infra/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~74의 보너스 폴리시를 이어, **아직 한 번도 손대지 않은 7줄짜리 순수 ResourceView 설정 화면(/procurement·/infra)** 을 선택(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783883105). 마운트 사본 꼬리 잘림을 피하려 두 파일 모두 python(encoding='utf-8')으로 직접 기록 — 102줄·101줄, 깨진문자(U+FFFD) 0 확인.

## 2026-07-13 (배치 74 — 배포 대기, 리스크 목록 '노출도'(발생×영향 점수) 컬럼 + '대응'(완화·비상계획) 컬럼·고위험 대응방안 미작성 경고)
- ⑥/④ **리스크 목록에 '노출도'·'대응' 컬럼 추가 — 등급 산출 근거 가시화 + 고위험 리스크의 대응방안 누락 색출** — /risks는 발생·영향을 `3`, `5` 같은 **맨숫자로만** 보여주고 등급(level) 배지를 옆에 붙이는데, 그 등급이 어떤 점수에서 나왔는지(=노출도)는 목록에 없어 사용자가 두 숫자를 눈으로 곱해야 했고, 더 큰 문제로 **리스크 관리의 본체인 대응방안(mitigation)·비상계획(contingency)이 작성됐는지가 목록에 전혀 드러나지 않아** "식별만 해두고 대응은 비어 있는 고위험 리스크"를 찾으려면 행을 하나씩 열어야 했음. ① **노출도** 컬럼: `발생가능성 × 영향도`(1~25점)를 계산해 점수 + 25점 만점 비율 바로 표시하고, 색상 임계값을 `lib/crud.ts`의 `RISK_TRANSFORM`(15↑ high · 8↑ medium)과 **동일 규칙**으로 맞춰 등급 배지와 어긋나지 않게 함(15↑ 빨강 #c0414f · 8↑ 주황 #d98a16 · 그 미만 초록 #2f8f5b, tabular-nums). hover 툴팁에 `발생가능성 4(높음) × 영향도 5(매우높음) = 20점 / 25점 · 등급 높음 — 최우선 대응 대상`으로 산출 근거를 명시. 종료(closed) 리스크는 회색·opacity 0.55로 톤다운. 발생·영향이 미입력이면 `미평가`로 구분 표기해 **평가 자체가 누락된 리스크**를 드러냄. ② **대응** 컬럼: 목록 API가 이미 전체 행(textarea 포함)을 돌려주므로 추가 fetch 없이 `mitigation`·`contingency`를 줄 단위로 파싱(글머리표 `- * · • 1.` 제거)해 `완화 3`(초록)·`비상 2`(회색) 칩 + 완화계획 첫 줄 미리보기(한 줄 ellipsis)를 표시하고, 툴팁에 번호 매긴 완화계획 앞 5건·비상계획 앞 3건을 노출 — 상세를 열지 않고 대응 충실도를 판단. **노출도 15점↑(또는 등급 high·critical)인데 완화계획이 비어 있고 종료되지도 않았으면 `⚠ 대응방안 미작성` 빨강 경고**(고위험 리스크의 대응방안 누락 = 감리·검수 지적 대상)를, 완화계획은 있으나 비상계획이 없는 고위험 리스크에는 `비상계획 없음`(주황)을 조용히 병기. 저위험·종료 리스크는 경보 대신 `미작성`(muted)으로만 표기해 과한 경고를 만들지 않음. ③ 발생·영향 셀을 `4/5` 형태로 바꾸고 hover 시 `발생가능성 4/5 — 높음` 한글 척도 라벨을 노출(1 매우낮음 ~ 5 매우높음), 폼의 두 숫자 필드에 동일한 척도 안내 hint를, 완화·비상계획 필드에는 작성 가이드 hint("한 줄에 조치 하나씩 — …")를 추가해 입력 시점부터 줄 단위 작성을 유도. 순수 계산·읽기 전용 파싱이라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/risks/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~73의 보너스 폴리시를 이어, 지금까지 한 번도 손대지 않은 화면 중 **정보 밀도가 가장 낮은 /risks(9줄, 순수 ResourceView 설정)** 를 선택(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_1783875905). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(26줄, columns 배열 중단, 21초 대기 후에도 미해소) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(93줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-13 (배치 73 — 배포 대기, 주간보고 '이번 주' 스코프 **결함 수정** + 다음 주 예정 블록 · 인터페이스 규격·미검증 승인 경고)
- ④ **[결함 수정] 주간보고(/weekly)의 '이번 주 완료'가 실제로는 '누적 완료 전체'를 보여주던 버그** — 파일에 주 범위 판정 함수 `inWeek()`가 정의되어 있는데도 **어디에서도 호출되지 않고**, 완료 목록이 `tasks.filter(t => t.status === 'done')`(상태만 검사)로 계산돼, 블록 제목은 "이번 주 완료"인데 **프로젝트 시작 이후 완료된 모든 업무가 그대로 나열**되고 있었음. 주가 지날수록 목록이 무한정 길어져 주간보고가 사실상 누적보고가 되고, 발주처에 "이번 주 실적"으로 제출하면 지난주 완료분이 중복 보고되는 실무상 치명적 오류. ① `within(값, from, to)` 공통 판정으로 정리하고 **`이번 주 완료` = 마감일(endDate)이 이번 주(월~일)에 드는 완료 업무**로 한정(마감일이 비어 있으면 최종 수정일 `updatedAt` 폴백 — 마감일 없이 완료 처리된 업무도 누락되지 않게). 잘못된 날짜 문자열은 `Number.isNaN` 가드로 배제. ② '진행중 / 다음 주 예정'이라는 이름으로 **진행중 업무와 미래 업무 전체를 한 덩어리로 뭉쳐 보여주던 블록을 `진행중`과 `다음 주 예정`(다음 주 월~일에 시작일 또는 마감일이 걸리는 미완료·비진행 업무) 두 개로 분리**하고, 각 블록 헤더에 대상 기간(`7.13 ~ 7.19`)을 명시해 스코프를 눈으로 확인 가능하게 함 — 종전에는 3개월 뒤 마감 업무까지 '다음 주 예정'에 섞여 들어왔음. ③ 상단 KPI에 **이번 주 완료**·**지연 업무**를 신설하고 기존 '완료 업무'는 의미대로 **누적 완료**로 개명(hover 시 산출 근거 툴팁), 미결 이슈에는 이번 주 신규 등록분을 `신규` 배지·툴팁으로 병기. ④ 각 블록의 빈 상태 문구를 도메인 맞춤으로 교체("이번 주 마감·완료된 업무가 없습니다 (누적 완료는 상단 KPI 참조)" 등)해 빈 목록이 데이터 누락으로 오인되지 않게 함. 클라이언트 계산·표시만 변경이라 데이터·API·스키마 무영향, 마이그레이션 불필요. — src/app/weekly/page.tsx
- ⑥/④ **인터페이스 목록에 '규격' 컬럼 + '미검증 승인' 경고 추가** — /interfaces는 코드·송수신·프로토콜·포맷·주기·담당·연동테스트·상태만 나열해, 인터페이스 정의서의 **본체인 연동 규격(spec)이 채워졌는지 목록에서 전혀 알 수 없고**, 연동테스트 상태와 결재 상태의 **모순(테스트 안 했는데 승인됨)** 도 드러나지 않았음. ① **규격** 컬럼: `spec`(textarea, 이미 목록 API가 반환)을 줄 단위로 파싱해 `N줄` 칩(테라코타) + 첫 줄 미리보기(ellipsis)를 표시하고 hover 툴팁에 번호 매긴 앞 8줄을 노출 — 상세를 열지 않고 규격 충실도를 판단. 규격이 비었는데 상태가 **`review`(결재요청)·`approved`(승인)** 이면 `⚠ 규격 미작성` 빨강 경고(규격 없이 결재된 인터페이스 정의서 = 감리 지적 1순위), `draft`(작성중)면 조용히 `미작성`(muted)으로만 표기해 작성 중 항목에 과한 경보를 만들지 않음. ② **연동테스트** 컬럼: 기존 단순 배지를 render로 바꿔, 상태가 `approved`인데 테스트가 `미실시`면 `⚠ 미검증 승인`(빨강), 테스트가 아직 `진행`이면 `승인·검증중`(주황)을 배지 옆에 병기하고 조치 안내를 툴팁으로 제공 — 검증 없이 승인된 인터페이스를 목록 스캔만으로 색출. ③ 담당자 필드를 자유 입력에서 `optionsFrom:'members'` 콤보로 바꿔 다른 도메인(업무·이슈)과 담당자 어휘를 일치시키고, 연동 규격 필드에 작성 가이드 hint("한 줄에 항목 하나씩 — 전문 포맷·필드·오류코드·재전송 정책 등")를 추가. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉. — src/app/interfaces/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 이번 실행은 배치50~72의 '보너스 폴리시'를 잇되, **표시 개선이 아닌 실제 계산 결함(주간보고 주 범위 미적용)을 발견해 ④ 안정화/품질 관점의 버그 수정을 우선 처리**함(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(exit 0, 에러 0). 작업 전 src 백업(/tmp/bak_1783868703). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(weekly 94줄·interfaces 26줄, 25초 대기 후에도 미해소) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(weekly 121줄·interfaces 67줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-12 (배치 72 — 배포 대기, 회의 목록 '참석자' 요약·'결정·조치' 컬럼 + 회의록 미작성 경고 · 캘린더 뷰)
- ⑥/⑧ **회의 목록에 '결정·조치' 컬럼 신설 + 참석자 요약 + 캘린더 대체뷰 연결** — /meetings는 코드·제목·일자·장소·참석자·차기 6개 컬럼뿐이라, **회의록의 실질(안건·결정사항·후속조치)이 목록에 전혀 드러나지 않아** 어떤 회의가 실제로 기록됐는지 확인하려면 행을 하나씩 열어야 했음. ① **결정·조치** 컬럼: 목록 API가 이미 전체 행(textarea 포함)을 돌려주므로 추가 fetch 없이 `decisions`·`actionItems`·`agenda`를 줄 단위로 파싱(글머리표 `- * · • 1.` 제거)해 `결정 3`(초록) · `조치 2`(주황) 칩으로 표시하고, hover 툴팁에 안건 건수 + 결정사항·후속조치 앞 5건을 번호 매겨 노출 — 목록 스캔만으로 회의록 충실도를 판단. **회의일이 이미 지났는데 결정사항·후속조치가 둘 다 비어 있으면 `⚠ 회의록 미작성`(빨강)** 으로 부각하고 툴팁에 "안건 N건은 등록되어 있습니다 — 회의록을 작성하세요"를 덧붙임(미작성 회의록은 감리·검수에서 바로 지적되는 항목). 아직 열리지 않은 예정 회의는 경보 대신 `안건 N건`(muted)만 표기해 과한 경고를 만들지 않음. 결정은 있는데 후속조치가 없는 지난 회의에는 `조치 없음`을 조용히 병기. ② **참석자** 컬럼: 쉼표·가운뎃점·줄바꿈으로 구분된 긴 원문이 셀을 밀어내던 것을 `N명` 칩(테라코타) + 앞 3명 미리보기(`홍길동 · 김철수 · 이영희 외 2`, 한 줄 ellipsis)로 축약하고 전체 명단은 번호 매긴 hover 툴팁으로 이동. 컬럼 키는 `attendees` 유지라 내보내기에는 원문이 그대로 실림. ③ 파일에 import만 되어 있고 **한 번도 연결되지 않은 채 방치돼 있던 `CalendarView`** 를 `meetingDate` 기준 캘린더 대체뷰(업무·이슈와 동일 패턴)로 실제 연결 — 월력에서 회의 일정을 보고 클릭해 상세를 열 수 있음. 읽기 전용 파싱·표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/meetings/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~71을 잇는 가독성 보너스 폴리시로 진행(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_1783861503). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(34줄, TS1005) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(75줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인. 최초 작성 시 CalendarView props를 `titleKey/onOpen`으로 잘못 넘긴 것을 실제 시그니처(`rows/dateKey/openDetail`)로 수정 후 통과.

## 2026-07-12 (배치 71 — 배포 대기, 감사 로그 검색·동작/영역 필터 + 한글 라벨·상대시각)
- ④/⑥ **감사 로그(/audit)에 검색·필터·한글 라벨·상대시각 도입** — 감사 로그는 최근 100건을 `TASKS_CREATE` 같은 **영문 원본 이벤트 문자열 그대로** 한 줄씩 나열하기만 해서, 필터도 검색도 없이 "어제 누가 이 산출물을 지웠는지"를 찾으려면 100줄을 눈으로 훑어야 했고, 전 화면 배지가 한글(LABEL)로 통일된 것과 달리 이 화면만 영문이 노출되던 마지막 구멍이었음. ① 이벤트를 `RESOURCE_ACTION` 규칙(lib/crud.ts)에 맞춰 파싱해 **동작**(생성 초록·수정 주황·삭제 빨강 배지)과 **영역**(tasks→업무, documents→산출물 등 21개 리소스 키의 한글명, 미등록 키는 원문 유지)으로 분리한 두 컬럼으로 재구성하고, 원본 이벤트 문자열은 대상 셀 hover 툴팁(`원본 이벤트: TASKS_CREATE`)으로 보존해 감사 추적성을 잃지 않음. ② **검색창**(사용자·영역·대상·동작 통합 매칭)과 **동작 필터**(옵션에 건수 표기 — `생성 (42)`), 로그에 실제 등장한 영역만 노출하는 **영역 필터** 드롭다운을 툴바에 추가하고, 배치19와 동일한 `표시건/전체건` 카운트(테라코타 강조 + 근거 툴팁)를 붙임 — 삭제 이력만 추리거나 특정 담당자의 변경만 뽑는 감사 실무 동선이 한 화면에서 끝남. ③ 시각을 `방금 전 / N분 전 / N시간 전 / N일 전`(30일 초과는 날짜) **상대시각**으로 바꾸고 절대시각은 툴팁으로 옮겨 최근 변경의 신선도를 즉시 스캔. ④ 로딩 스켈레톤(.skel 5행)을 넣어 ④ 항목의 로딩 일관화 규칙에 맞추고, 빈 상태를 **기록 자체가 없음** vs **필터 결과 없음**으로 분기해 안내 문구를 다르게 노출. 읽기 전용 표시·클라이언트 필터라 API·데이터·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/audit/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — 동적 폼 빌더)뿐이라 자동 개발 금지 대상 → 배치50~70을 잇는 가독성 보너스 폴리시로 진행(신규 테이블 미접촉). 이번엔 지금까지 한 번도 손대지 않은 화면 중 정보 밀도가 가장 낮은 /audit을 선택. ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_1783854309). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(26줄, relTime 주석 중단) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(133줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인. CHANGELOG는 마운트 사본이 UTF-8 중간에서 잘려 있어 호스트 원본에 직접 Edit로 기록.

## 2026-07-12 (배치 70 — 배포 대기, 산출물 양식 '항목'·'적용 산출물' 컬럼)
- ⑦/⑥ **산출물 양식 목록에 '항목'(양식 구성 미리보기)·'적용 산출물'(사용 현황) 컬럼 추가** — 배치49에서 form_definitions 도메인을 만들었지만 목록은 코드·양식명·대상 산출물·상태 4개 컬럼뿐이라, **양식의 존재 이유인 '항목 구성'(fields)이 목록에 전혀 보이지 않아** 어떤 양식이 실제로 채워졌는지 확인하려면 행을 하나씩 열어야 했고, 정의한 양식이 실제로 쓰이는지도 알 수 없었던 누락을 메움. ① **항목** 컬럼: 줄바꿈 구분 텍스트인 `fields`를 파싱해 `N개` 칩(테라코타 톤) + 앞 3개 미리보기(`항목1 · 항목2 · 항목3 외 2`, 한 줄 ellipsis)를 표시하고 hover 툴팁에 번호 매긴 전체 항목 목록을 노출 — 상세를 열지 않고 양식 구성을 그 자리에서 확인. 항목이 비었는데 상태가 **`active`(사용)** 이면 `⚠ 항목 미정의` 빨강 경고(표준 양식으로 배포됐는데 내용이 없는 상태 = 표준화의 핵심 결함), `draft`·`archived`면 조용히 `미정의`(muted)로만 표기해 작성 중인 양식에 과한 경보를 만들지 않음. ② **적용 산출물** 컬럼: 배치60·63~66과 동일한 페이지-로컬 보조 fetch 집계 패턴으로 `/api/documents?projectId=`를 한 번 읽어 `document.type` 기준으로 묶고, 양식의 `targetType`과 같은 유형의 산출물 **승인/전체** 건수를 표시(hover 시 `'설계서' 산출물 총 12건 · 승인 7건 · 결재요청 3건 · 작성중 2건`, 반려 보유 시 `⚠N` 빨강 칩). 해당 유형 산출물이 0건이면 `미사용`으로 구분 표기해 **정의만 해두고 실제로는 쓰이지 않는 유령 양식**을 목록 스캔만으로 식별. form_definitions의 `targetType` 옵션과 documents의 `type` 옵션이 같은 산출물 유형 어휘를 공유하는 점을 활용(문자열 매칭). 읽기 전용 파싱·집계라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/form-definitions/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식 — **동적 폼 빌더**)뿐이라 자동 개발 금지 대상 → 배치50~69를 잇는 가독성 보너스 폴리시로 진행(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_1783847100). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(29줄, 집계 루프 중단) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(82줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-12 (배치 69 — 배포 대기, 백로그·스프린트 진척 바 + 이슈 상태 배지)
- ⑧/③ **백로그 화면에 스프린트 진척 바·이슈 상태 배지 추가** — /backlog는 스프린트 헤더에 `N건 · N pts`만 보여주고 **이슈 행에는 상태(open/in_progress/resolved/closed)가 아예 없어**, 진행 중인 스프린트가 얼마나 소화됐는지·어떤 이슈가 아직 열려 있는지를 목록에서 전혀 알 수 없고 매번 이슈 화면으로 넘어가야 했던 누락을 메움(스크럼 보드의 가장 기본적인 정보). ① 스프린트 헤더에 **진척 바**(`SprintProgress`, 표준 `.bar` 90px + 퍼센트, 100% 초록 #2f8f5b · 50%↑ 테라코타 var(--brand) · 그 미만 회색 #9a9a9a)를 넣되 분모를 **스토리포인트 합**으로 잡고(스크럼 정석), 포인트가 전부 미입력인 스프린트에서는 0으로 나눠 NaN이 되지 않도록 **건수 기준으로 자동 대체**해 hover 툴팁에 `총 8건 · 완료 3건 · 남은 5건 / 포인트 13/34 pts (38%)` 또는 `스토리포인트 미입력 — 건수 기준 38%`로 근거를 명시(aria-label 동일). ② 이슈 행에 `<Pill v={i.status} />` 상태 배지를 우선순위 옆에 추가하고, 완료(resolved·closed) 이슈는 행 opacity 0.62 + 제목 취소선으로 톤다운해 남은 일감이 시각적으로 도드라지게 함. ③ 스프린트 상태 배지가 `planned`·`active` 영문 원문을 그대로 노출하던 것을 lib/ui의 `LABEL` 한글 라벨(계획·진행·완료)로 교체해 전 화면 배지 표기와 일관화(배치18 필터 한글화와 동일 취지). 완료 판정은 이슈 목록·칸반과 같은 `resolved`·`closed` 기준. 읽기 전용 표시라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/backlog/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식)뿐이라 주말 자동 개발 금지 대상 → 배치50~68을 잇는 가독성 보너스 폴리시로 진행(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_wk_1783836135). OneDrive 마운트 동기화 지연으로 마운트 사본 꼬리 잘림(91줄, SprintProgress 직후 중단) → 호스트 원본(Write) 내용 기준으로 python(utf-8)으로 마운트 사본을 동일 내용(113줄)으로 재구성해 0에러·깨진문자(U+FFFD) 0 재확인.

## 2026-07-12 (배치 68 — 배포 대기, 기성고·스냅샷 목록 '괴리'(계획 대비 실적) 컬럼 + 기성 적정성 진단)
- ③/⑥ **기성고·스냅샷 목록에 '괴리' 컬럼 추가 — 계획 대비 실적 편차(%p)와 기성 과다/과소 청구 경고** — /snapshots는 계획·실적·기성률을 각각 진척 바로 보여주지만 셋의 **관계**(계획보다 뒤졌는지, 기성 청구가 실적에 비해 앞서는지)는 사람이 세 막대를 눈으로 빼서 계산해야 했음. 실적−계획을 %p로 계산한 **괴리 컬럼**을 실적과 기성률 사이에 배치(지연 10%p↑ 빨강 #c0414f+⚠ · 지연 주황 #d98a16 · 선행/정시 초록 #2f8f5b, tabular-nums, `+3%p`/`-12%p` 부호 표기). 여기에 **기성 적정성 진단**을 얹어, 기성률이 실적 진척을 10%p 이상 앞서면 `기성 과다`, 10%p 이상 뒤지면 `기성 과소` 칩(테라코타 톤)을 붙이고 hover 툴팁에 `계획 60% · 실적 48% / 계획보다 12%p 지연 / 기성률이 실적보다 15%p 앞섬 — 과다청구 검토`처럼 근거를 명시 — 발주처 기성 검수에서 문제가 될 수 있는 스냅샷을 목록 스캔만으로 짚어낼 수 있음. 계획·실적 중 하나라도 미입력이면 `—`(입력 안내 툴팁), 기성률 미입력이면 괴리만 표시. 세 진척 바에 반복되던 JSX를 공통 `BAR` 헬퍼로 묶고 `.bar`의 min-width 80px를 44px로 축소해 컬럼 1개 추가에도 가로폭이 늘지 않도록 함. 순수 계산·표시 render 컬럼(`gap`)이라 데이터·API·스키마 무영향, 마이그레이션 불필요. 중앙 ResourceView 미접촉(단일 파일). — src/app/snapshots/page.tsx
- ⑥ 안전 백로그·⑧ monday UX·⑤ 테스트 모두 [x] 소진, ROADMAP 잔여 `[ ]`는 대형/주간 수동(⑦·라인17 커스텀 산출물 양식)뿐이라 주말 자동 개발 금지 대상 → 배치50~67을 잇는 가독성 보너스 폴리시로 진행(신규 테이블 미접촉). ROADMAP 변경 없음.
- 검증: `tsc --noEmit` 통과(에러 0). 작업 전 src 백업(/tmp/bak_wk_1783821737). 이번 실행은 마운트 사본 꼬리 잘림 없이 정상(59줄, 호스트 원본 Read로 재확인) · 깨진문자(U+FFFD) 0.

## 2026-07-12 (배치 67 — 배포 대기, 업무(WBS) 목록 '공수'(계획 대비 실제) 컬럼)
- ③/④ **업무 목록에 '공수' 컬럼 추가 — 계획 대비 실제 공수·소진율 바·초과 경고** — tasks는 `plannedHours`(계획 공수)·`actualHours`(실제 공수) 필드를 폼에 갖고 있고 이 값이 EVM의 **AC·CPI 계산에 그대로 쓰이는데**, 정작 목록 컬럼에는 공수가 **아예 없어** 입력한 공수를 확인하거나 계획 대비 초과를 비교하려면 업무를 하나씩 열어야 했던 누락을 메움(이슈 목록은 이미 '공수(h)' 컬럼 보유). 진척과 마감 사이에 `실제 / 계획h` 숫자(tabular-nums) + **소진율 바**(표준 `.bar`, 계획 초과 빨강 #c0414f · 소진율 80%↑ 주황 #d98a16 · 그 외 초록 #2f8f5b, `.bar`의 min-width 80px를 inline mi