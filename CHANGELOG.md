# PRISM PMS — 변경 이력 (CHANGELOG)

> 야간 자동 개발이 매 실행마다 최신 항목을 **맨 위에** 추가합니다.
> 아침에 `배포.ps1` 실행 → GitHub 푸시 → Vercel 자동배포.

## 2026-07-01 (수동 세션 누적분 — 아침 배포 대기 중)
- v16~v22 대규모 고도화: 실제 GOWON 로고(서버 프록시), ⌘K 전역검색, 프로젝트 상세 워크스페이스,
  상용 차트 리디자인, 인터랙티브 간트(+드래그 생성·줌·의존선·마일스톤·진척드래그),
  내 작업/주간보고/RTM/산출물 결재, 인력 드롭다운, 알림 자동화, 대시보드 위젯, 배지 한글화,
  모바일 반응형 하드닝(뷰포트·상단바·검색복구).
- ⚠️ DB 컬럼 추가분(req_code, predecessor, approved_at) — 배포 후 사이트에서 관리자 마이그레이션 필요.

## 2026-07-02 (주간 배치 A — 배포 대기)
- 안정화: 모든 폼에서 시작일 > 마감일 입력 방지(친화적 오류 메시지). — components/ResourceView.tsx
- 간트: 마감 초과(지연) 작업 막대를 빨강 테두리로 자동 강조. — components/views.tsx
- 간트: 막대에 담당자 이니셜 칩 표시. — components/views.tsx
- 검증: tsc --noEmit 타입체크 통과(에러 0).

## 2026-07-02 (주간 배치 B·C — 배포 대기)
- 리포트/분석: **담당자별 업무 부하(리소스) 뷰** 신규 (/workload, 사이드바 '업무 부하'). 인력별 진행업무+미결이슈 합산·부하바. — app/api/workload, app/workload, Shell
- 품질: 상세 슬라이드오버·모달을 **ESC 키로 닫기**. — components/ResourceView.tsx
- 검증: tsc --noEmit 통과(에러 0).

## 2026-07-02 (주간 배치 D — 배포 대기)
- 리포트/분석: 프로젝트 상세에 **일정 성과(SPI) 카드** (계획진척 vs 실제진척 + SPI 색상 판정). — app/api/project-summary, app/projects/[id]
- 모바일/UX: 표 **가로 스크롤 그림자 힌트**(끝에서 사라지는 스크롤 셰도우). — app/globals.css
- 검증: tsc --noEmit 통과(에러 0). DB 변경 없음(마이그레이션 불필요).

## 2026-07-02 (주간 배치 E — 배포 대기)
- 간트: **임계경로(Critical Path) 강조** — 가장 늦게 끝나는 작업에서 선행 체인을 따라 주경로를 빨강 링으로 표시(+범례·툴팁). — components/views.tsx
- 검증: tsc --noEmit 통과(에러 0). DB 변경 없음.

## 2026-07-02 (주간 배치 F — 배포 대기)
- 리포트/분석: 프로젝트 상세에 **이슈 우선순위·리스크 등급 분포** 미니 바 카드 추가. — app/api/project-summary, app/projects/[id]
- 검증: tsc --noEmit 통과(에러 0). DB 변경 없음.

## 2026-07-02 (주간 배치 G — 배포 대기)
- 안정화: API 공통 에러 핸들러가 DB 오류코드(중복/필수/형식/FK/체크)를 **명확한 한글 메시지**로 변환. — lib/http.ts
- 검증: tsc --noEmit 통과(에러 0). DB 변경 없음.

## 2026-07-02 (주간 배치 H — 배포 대기)
- 접근성: 수정/삭제/닫기/알림 아이콘 버튼에 aria-label 추가. — ResourceView, Shell
- 검증: tsc 통과. DB 변경 없음.

## 2026-07-02 (주간 배치 I — 배포 대기 · ⚠️마이그레이션 필요)
- 핵심 신규: **테스트 관리** 도메인 추가 — 테스트 케이스(절차·기대결과·유형·우선순위·연계요구사항) + **개발자→PL→PM 순차 검증 상태** + '검증 파이프라인' 칸반 + 결과(통과/실패/블록). 사이드바 '테스트'. — db/schema, lib/configs, app/api/tests, app/tests, Shell, lib/ui
- UI: 로고 하단 'by GOWON' 문구 제거. — components/Logo.tsx
- ⚠️ 신규 테이블 tests — 배포 후 관리자 마이그레이션 실행 필요(자동 실행 예정).
- 검증: tsc --noEmit 통과(에러 0).

## 2026-07-02 (주간 배치 J — 성능 · 배포 대기)
- 성능: 대시보드 데이터 요청을 **9회 → 1회 집계 API(/api/dashboard, 서버측 병렬 조회)** 로 통합. 초기 로딩 대폭 단축.
- 성능: 알림 자동생성(쓰기)을 대시보드 진입마다 → **1시간에 1회로 제한**(불필요한 DB 쓰기 제거).
- 검증: tsc --noEmit 통과(에러 0). DB 변경 없음.

## 2026-07-02 (주간 배치 K — 성능 핵심 · 배포 대기)
- 성능(핵심): Vercel 함수 리전을 **싱가포르(sin1)** 로 고정 — Neon DB(싱가포르)와 동일 리전에 배치해 쿼리 왕복지연을 ~200ms→~5ms로 대폭 감소. — vercel.json
- 검증: 설정 파일(빌드 무관). Vercel 배포 시 리전 적용.

## 2026-07-02 (주간 배치 L — 배포 대기)
- 계정: **내 계정 표시 이름 변경** 기능 추가(설정 > 내 계정, /api/profile). 로그인 계정별 개별 이름. — app/api/profile, app/settings
- 검증: tsc 통과. DB 변경 없음.

## 2026-07-02 (주간 배치 M — 배포 대기)
- 데모: 데모 데이터에 **테스트 케이스 5건**(검증단계·결과·요구사항연계) 추가 — 채우기 후 테스트 화면 즉시 표시. — lib/demoSeed.ts
- UX: '데모 데이터 채우기' 완료 시 **자동 새로고침**. — app/settings
- 계정 메뉴: 우측 상단 계정 드롭다운을 PMS용으로 강화(아이콘 헤더·이름/이메일만, 내작업/알림/설정/감사로그/로그아웃, 부적합 항목 제외). — components/Shell.tsx
- 검증: tsc 통과. DB 변경 없음.

## 2026-07-02 (주간 배치 N — 배포 대기)
- 데모(핵심 수정): 데모 데이터를 **모든 프로젝트에 대해, 비어있는 항목만** 채우도록 재작성 — 이제 어느 프로젝트를 선택해도 단계·요구사항·이슈·리스크·업무·테스트·산출물·회의·인프라 등 모두 표시. 테스트 케이스 포함. — lib/demoSeed.ts
- 배포 스크립트: PowerShell 한글 깨짐 수정(UTF-8 BOM + 콘솔 UTF-8). — 배포.ps1
- 검증: tsc 통과. DB 변경 없음.

## 2026-07-02 (주간 배치 O — 배포 대기)
- 버그: 설정>내 계정 이름 저장 시 화면이 깜박이며 빈칸이 되던 문제 수정(불필요한 새로고침 제거, 입력값 유지). — app/settings
- UX: '데모 데이터 채우기' 완료 후 **테스트 화면으로 이동**해 데이터가 바로 보이도록. — app/settings
- 검증: tsc 통과. DB 변경 없음.

## 2026-07-02 (주간 배치 P — 배포 대기 · ⚠️마이그레이션 필요)
- 테스트: 결함 트래커 수준으로 강화 — **보고자·기한·진척률** 필드 및 목록 컬럼 추가(이슈·결함과 동일 수준의 추적). — db/schema, lib/configs, app/tests, migrate
- ⚠️ tests 신규 컬럼(reporter, due_date, progress) — 배포 후 마이그레이션 필요.
- 검증: tsc 통과.

## 2026-07-02 (주간 배치 Q — 배포 대기)
- RTM: **테스트 커버리지 연계** — 요구사항별 '연계 테스트'(통과/실패 색상) 컬럼 추가, 커버리지 판정에 테스트 통과·실패 반영(테스트 실패 시 빨강). — app/rtm/page.tsx
- 검증: tsc 통과. DB 변경 없음.

<!-- 야간 자동 개발은 이 줄 위에 새 날짜 블록을 추가하세요 -->
