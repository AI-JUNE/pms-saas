# 품질 기준 (QUALITY BAR)

> 상용 서비스는 "동작한다"가 아니라 **"경쟁 제품보다 낫다"**가 기준이다.
> 이 문서의 항목을 만족하지 못하면 기능이 완성된 것이 아니다.

## 1. 반드시 동작해야 하는 것 (기능 정상성)
- [ ] 핵심 사용자 흐름이 **처음부터 끝까지** 끊기지 않는다(가입→사용→결과 확인)
- [ ] 새로고침·뒤로가기·중복 클릭에도 상태가 깨지지 않는다
- [ ] 네트워크 실패·서버 오류 시 **사용자가 이해할 수 있는 안내**가 뜬다(빈 화면·무반응 금지)
- [ ] 빈 상태(데이터 0건)에서 안내와 다음 행동이 제시된다
- [ ] 느린 응답 구간에 로딩 표시가 있다(멈춘 것처럼 보이지 않는다)
- [ ] 잘못된 입력에 어느 항목이 왜 틀렸는지 인라인으로 알려준다

## 2. 경쟁 제품보다 나아야 하는 것 (차별점)
각 제품은 아래 중 최소 2개에서 **명확히 우위**여야 한다.
- **속도** — 첫 화면 표시와 주요 동작 응답이 체감상 즉각적
- **쉬움** — 설명 없이 첫 사용에 성공. 클릭 수가 경쟁 제품보다 적다
- **한국 현업 적합성** — 국내 업무 관행·용어·서식에 맞다(해외 제품의 약점)
- **끊김 없는 연결** — 채널·화면이 바뀌어도 맥락이 유지된다
- **운영 자동화** — 반복 업무(보고·집계·정산)가 자동으로 처리된다

## 3. 신뢰를 깨는 것 (절대 금지)
- 근거 없는 성과 수치, 허위 고객사·후기
- 오류를 삼키고 아무 일 없는 척하기(사용자는 실패한 줄 모른 채 떠난다)
- 개인정보가 화면·로그·오류 리포트에 그대로 노출
- 되돌릴 수 없는 동작에 확인 절차 없음(삭제·발송·결제)

## 4. 기능 완료 판정 (Definition of Done)
아래를 모두 만족해야 `[x]` 로 표시한다.
1. 빌드·타입 검증 통과
2. 테스트 동반(정상 경로 + 실패 경로 최소 1건)
3. 빈 상태·오류 상태 화면이 있다
4. 모바일 화면에서 깨지지 않는다
5. 키보드만으로 조작 가능하고 스크린리더 라벨이 있다
6. 개인정보를 다루면 마스킹·권한 확인을 거친다

## 5. 점검 방식
- 기능 추가 시 위 4번 체크리스트를 프롬프트에 명시적으로 확인
- 매일 E2E 스모크 검증이 라이브 서비스에 실제 요청을 보내 동작을 확인
- 실패 발견 시 새 기능보다 **수정을 우선**한다
